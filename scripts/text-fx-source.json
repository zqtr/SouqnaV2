{"effects": [{"index": "01", "slug": "aurora", "name": "Borealis", "stage": "<div class=\"fx-aurora\">AURORA</div>"}, {"index": "02", "slug": "glitch", "name": "Glitchcore", "stage": "<div class=\"fx-glitch\" data-text=\"GLITCH\">GLITCH</div>"}, {"index": "03", "slug": "typewriter", "name": "Teletype", "stage": "<div class=\"fx-typewriter\">TYPEWRITER</div>"}, {"index": "04", "slug": "neon", "name": "Neon-Haus", "stage": "<div class=\"fx-neon\">NEON</div>"}, {"index": "05", "slug": "liquid", "name": "Aqua-Fill", "stage": "<div class=\"fx-liquid\">LIQUID</div>"}, {"index": "06", "slug": "chrome", "name": "Chromia", "stage": "<div class=\"fx-chrome\">CHROME</div>"}, {"index": "07", "slug": "focus", "name": "Lens-Drift", "stage": "<div class=\"fx-focus\" role=\"img\" aria-label=\"FOCUS\"><b aria-hidden=\"true\" style=\"--i:0\">F</b><b aria-hidden=\"true\" style=\"--i:1\">O</b><b aria-hidden=\"true\" style=\"--i:2\">C</b><b aria-hidden=\"true\" style=\"--i:3\">U</b><b aria-hidden=\"true\" style=\"--i:4\">S</b></div>"}, {"index": "08", "slug": "wave", "name": "Tidal-Type", "stage": "<div class=\"fx-wave\" role=\"img\" aria-label=\"WAVE\"><b aria-hidden=\"true\" style=\"--i:0\">W</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">V</b><b aria-hidden=\"true\" style=\"--i:3\">E</b></div>"}, {"index": "09", "slug": "sliced", "name": "Bisect", "stage": "<div class=\"fx-sliced\" data-text=\"SLICED\">SLICED</div>"}, {"index": "10", "slug": "decoder", "name": "Cipher", "stage": "<div class=\"fx-decoder\" role=\"img\" aria-label=\"DECODER\"></div>"}, {"index": "11", "slug": "scanner", "name": "Redactor", "stage": "<div class=\"fx-scanner\">SCANNER</div>"}, {"index": "12", "slug": "ember", "name": "Emberglow", "stage": "<div class=\"fx-ember\">EMBER</div>"}, {"index": "13", "slug": "echo", "name": "Echo-Verse", "stage": "<div class=\"fx-echo\" data-text=\"ECHO\">ECHO</div>"}, {"index": "14", "slug": "extrude", "name": "Deep-Type", "stage": "<div class=\"fx-extrude\">DEPTH</div>"}, {"index": "15", "slug": "contour", "name": "Wireframe", "stage": "<svg class=\"fx-contour\" viewBox=\"0 0 260 56\" role=\"img\" aria-label=\"OUTLINE\"><text x=\"50%\" y=\"50%\" dominant-baseline=\"central\" text-anchor=\"middle\">OUTLINE</text></svg>"}, {"index": "16", "slug": "spectrum", "name": "Prisma", "stage": "<div class=\"fx-spectrum\" role=\"img\" aria-label=\"SPECTRUM\"><b aria-hidden=\"true\" style=\"--i:0\">S</b><b aria-hidden=\"true\" style=\"--i:1\">P</b><b aria-hidden=\"true\" style=\"--i:2\">E</b><b aria-hidden=\"true\" style=\"--i:3\">C</b><b aria-hidden=\"true\" style=\"--i:4\">T</b><b aria-hidden=\"true\" style=\"--i:5\">R</b><b aria-hidden=\"true\" style=\"--i:6\">U</b><b aria-hidden=\"true\" style=\"--i:7\">M</b></div>"}, {"index": "17", "slug": "jitter", "name": "Jitterbug", "stage": "<div class=\"fx-jitter\">JITTER</div>"}, {"index": "18", "slug": "anaglyph", "name": "Anaglyph-3D", "stage": "<div class=\"fx-anaglyph\">DEPTH-3D</div>"}, {"index": "19", "slug": "flap", "name": "Split-Flap", "stage": "<div class=\"fx-flap\" role=\"img\" aria-label=\"DEPART\"><b aria-hidden=\"true\" style=\"--i:0\">D</b><b aria-hidden=\"true\" style=\"--i:1\">E</b><b aria-hidden=\"true\" style=\"--i:2\">P</b><b aria-hidden=\"true\" style=\"--i:3\">A</b><b aria-hidden=\"true\" style=\"--i:4\">R</b><b aria-hidden=\"true\" style=\"--i:5\">T</b></div>"}, {"index": "20", "slug": "crt", "name": "Phosphor", "stage": "<div class=\"fx-crt\">CRT_MODE</div>"}, {"index": "21", "slug": "pop", "name": "Pop-Riot", "stage": "<div class=\"fx-pop\" role=\"img\" aria-label=\"POP!\"><b aria-hidden=\"true\" style=\"--i:0\">P</b><b aria-hidden=\"true\" style=\"--i:1\">O</b><b aria-hidden=\"true\" style=\"--i:2\">P</b><b aria-hidden=\"true\" style=\"--i:3\">!</b></div>"}, {"index": "22", "slug": "spotlight", "name": "Limelight", "stage": "<div class=\"fx-spotlight\">SPOTLIGHT</div>"}, {"index": "23", "slug": "elastic", "name": "Rubber-Band", "stage": "<div class=\"fx-elastic\">BOING</div>"}, {"index": "24", "slug": "mirror", "name": "Still-Water", "stage": "<div class=\"fx-mirror\">MIRROR</div>"}, {"index": "25", "slug": "ransom", "name": "Ransom-Note", "stage": "<div class=\"fx-ransom\" role=\"img\" aria-label=\"RANSOM\"><b aria-hidden=\"true\" style=\"--i:0\">R</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">N</b><b aria-hidden=\"true\" style=\"--i:3\">S</b><b aria-hidden=\"true\" style=\"--i:4\">O</b><b aria-hidden=\"true\" style=\"--i:5\">M</b></div>"}, {"index": "26", "slug": "melt", "name": "Meltdown", "stage": "<div class=\"fx-melt\" role=\"img\" aria-label=\"MELT\"><b aria-hidden=\"true\" style=\"--i:0\">M</b><b aria-hidden=\"true\" style=\"--i:1\">E</b><b aria-hidden=\"true\" style=\"--i:2\">L</b><b aria-hidden=\"true\" style=\"--i:3\">T</b></div>"}, {"index": "27", "slug": "heartbeat", "name": "Cardio", "stage": "<div class=\"fx-heartbeat\">PULSE</div>"}, {"index": "28", "slug": "marker", "name": "Hi-Liter", "stage": "<div class=\"fx-marker\">MARKED</div>"}, {"index": "29", "slug": "sundial", "name": "Sundial", "stage": "<div class=\"fx-sundial\">SHADOW</div>"}, {"index": "30", "slug": "negative", "name": "Negativ", "stage": "<div class=\"fx-negative\">INVERTED</div>"}, {"index": "31", "slug": "hologram", "name": "Holograph", "stage": "<div class=\"fx-hologram\" data-text=\"HOLOGRAM\">HOLOGRAM</div>"}, {"index": "32", "slug": "foil", "name": "Gold-Foil", "stage": "<div class=\"fx-foil\">LUXE</div>"}, {"index": "33", "slug": "pixel", "name": "Pixel-Sort", "stage": "<div class=\"fx-pixel\" data-text=\"PIXELS\">PIXELS</div>"}, {"index": "34", "slug": "starlight", "name": "Starlight", "stage": "<div class=\"fx-starlight\">STARDUST</div>"}, {"index": "35", "slug": "blueprint", "name": "Blueprint", "stage": "<div class=\"fx-blueprint\">DRAFT</div>"}, {"index": "36", "slug": "vapor", "name": "Vapor-Trail", "stage": "<div class=\"fx-vapor\" data-text=\"VAPOR\">VAPOR</div>"}, {"index": "37", "slug": "kinetic", "name": "Kinetic-Type", "stage": "<div class=\"fx-kinetic\" role=\"img\" aria-label=\"KINETIC\"><b aria-hidden=\"true\" style=\"--i:0\">K</b><b aria-hidden=\"true\" style=\"--i:1\">I</b><b aria-hidden=\"true\" style=\"--i:2\">N</b><b aria-hidden=\"true\" style=\"--i:3\">E</b><b aria-hidden=\"true\" style=\"--i:4\">T</b><b aria-hidden=\"true\" style=\"--i:5\">I</b><b aria-hidden=\"true\" style=\"--i:6\">C</b></div>"}, {"index": "38", "slug": "blackout", "name": "Blackout", "stage": "<div class=\"fx-blackout\" data-text=\"CLASSIFIED\">CLASSIFIED</div>"}, {"index": "39", "slug": "magnetic", "name": "Magnetic", "stage": "<div class=\"fx-magnetic\" role=\"img\" aria-label=\"MAGNET\"><b aria-hidden=\"true\" style=\"--i:0\">M</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">G</b><b aria-hidden=\"true\" style=\"--i:3\">N</b><b aria-hidden=\"true\" style=\"--i:4\">E</b><b aria-hidden=\"true\" style=\"--i:5\">T</b></div>"}, {"index": "40", "slug": "mesh", "name": "Luma-Mesh", "stage": "<div class=\"fx-mesh\">MESH</div>"}, {"index": "41", "slug": "iridescent", "name": "Iridescent", "stage": "<div class=\"fx-iridescent\">IRIS</div>"}, {"index": "42", "slug": "glass", "name": "Glass-Type", "stage": "<div class=\"fx-glass\" data-text=\"GLASS\">GLASS</div>"}, {"index": "43", "slug": "datastream", "name": "Datastream", "stage": "<div class=\"fx-datastream\">STREAM</div>"}, {"index": "44", "slug": "orbit", "name": "Orbitals", "stage": "<div class=\"fx-orbit\" role=\"img\" aria-label=\"ORBIT\"><b aria-hidden=\"true\" style=\"--i:0\">O</b><b aria-hidden=\"true\" style=\"--i:1\">R</b><b aria-hidden=\"true\" style=\"--i:2\">B</b><b aria-hidden=\"true\" style=\"--i:3\">I</b><b aria-hidden=\"true\" style=\"--i:4\">T</b></div>"}, {"index": "45", "slug": "prismcut", "name": "Prism-Cut", "stage": "<div class=\"fx-prismcut\" data-text=\"PRISM\">PRISM</div>"}, {"index": "46", "slug": "softblur", "name": "Soft-Blur", "stage": "<div class=\"fx-softblur\">FOCUS</div>"}, {"index": "47", "slug": "laser", "name": "Laser-Cut", "stage": "<div class=\"fx-laser\" data-text=\"LASER\">LASER</div>"}, {"index": "48", "slug": "microchip", "name": "Microchip", "stage": "<div class=\"fx-microchip\">CIRCUIT</div>"}, {"index": "49", "slug": "heatmap", "name": "Heatmap", "stage": "<div class=\"fx-heatmap\">THERMAL</div>"}, {"index": "50", "slug": "parallax", "name": "Parallax", "stage": "<div class=\"fx-parallax\" role=\"img\" aria-label=\"LAYERS\"><b aria-hidden=\"true\" style=\"--i:0\">L</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">Y</b><b aria-hidden=\"true\" style=\"--i:3\">E</b><b aria-hidden=\"true\" style=\"--i:4\">R</b><b aria-hidden=\"true\" style=\"--i:5\">S</b></div>"}, {"index": "51", "slug": "inktrap", "name": "Ink-Trap", "stage": "<div class=\"fx-inktrap\" data-text=\"INK\">INK</div>"}, {"index": "52", "slug": "topographic", "name": "Topographic", "stage": "<div class=\"fx-topographic\">TERRAIN</div>"}, {"index": "53", "slug": "signal", "name": "Signal-Noise", "stage": "<div class=\"fx-signal\" data-text=\"SIGNAL\">SIGNAL</div>"}, {"index": "54", "slug": "portal", "name": "Portal", "stage": "<div class=\"fx-portal\" data-text=\"PORTAL\">PORTAL</div>"}, {"index": "55", "slug": "tiltshift", "name": "Tilt-Shift", "stage": "<div class=\"fx-tiltshift\" data-text=\"SHIFT\">SHIFT</div>"}, {"index": "56", "slug": "duotone", "name": "Duotone", "stage": "<div class=\"fx-duotone\" data-text=\"DUO\">DUO</div>"}, {"index": "57", "slug": "glyphrain", "name": "Glyph-Rain", "stage": "<div class=\"fx-glyphrain\" role=\"img\" aria-label=\"GLYPHS\"><b aria-hidden=\"true\" style=\"--i:0\">G</b><b aria-hidden=\"true\" style=\"--i:1\">L</b><b aria-hidden=\"true\" style=\"--i:2\">Y</b><b aria-hidden=\"true\" style=\"--i:3\">P</b><b aria-hidden=\"true\" style=\"--i:4\">H</b><b aria-hidden=\"true\" style=\"--i:5\">S</b></div>"}], "prompts": {"aurora": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Borealis\" text effect applied to the word \"AURORA\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-aurora\">AURORA</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 01 Borealis \u2014 aurora gradient drifting through the letters */\n.fx-aurora {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  background: linear-gradient(115deg, var(--ink-2), var(--ink-3), var(--ink-2), var(--ink-3)) 0 0 / 300% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-aurora 7s linear infinite;\n}\n@keyframes fx-aurora {\n  0%   { background-position: 0% 50%;   filter: hue-rotate(0deg); }\n  100% { background-position: 300% 50%; filter: hue-rotate(360deg); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"AURORA\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "glitch": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Glitchcore\" text effect applied to the word \"GLITCH\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-glitch\" data-text=\"GLITCH\">GLITCH</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 02 Glitchcore \u2014 RGB channel-split glitch with sliced jumps */\n.fx-glitch {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n}\n.fx-glitch::before, .fx-glitch::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n}\n.fx-glitch::before {\n  color: var(--ink-2);\n  clip-path: inset(0 0 55% 0);\n  animation: fx-glitch-a 2.6s steps(1) infinite;\n}\n.fx-glitch::after {\n  color: var(--ink-3);\n  clip-path: inset(45% 0 0 0);\n  animation: fx-glitch-b 2.6s steps(1) infinite;\n}\n@keyframes fx-glitch-a {\n  0%, 12%, 20%, 55%, 100% { transform: translate(0); opacity: 0; }\n  13% { transform: translate(-5px, -2px); opacity: .9; }\n  16% { transform: translate(4px, 1px);   opacity: .9; }\n  48% { transform: translate(-3px, 2px);  opacity: .9; }\n  51% { transform: translate(2px, -1px);  opacity: .9; }\n}\n@keyframes fx-glitch-b {\n  0%, 14%, 22%, 60%, 100% { transform: translate(0); opacity: 0; }\n  15% { transform: translate(5px, 2px);   opacity: .9; }\n  18% { transform: translate(-4px, -1px); opacity: .9; }\n  52% { transform: translate(3px, -2px);  opacity: .9; }\n  56% { transform: translate(-2px, 1px);  opacity: .9; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"GLITCH\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "typewriter": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Teletype\" text effect applied to the word \"TYPEWRITER\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-typewriter\">TYPEWRITER</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 03 Teletype \u2014 typewriter with blinking caret (word must match the ch width) */\n.fx-typewriter {\n  font: 600 24px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: 0;\n  color: var(--ink);\n  white-space: nowrap;\n  overflow: hidden;\n  width: 10ch; /* = number of characters typed */\n  border-right: .55ch solid var(--ink-2);\n  animation: fx-type 3.6s steps(10) infinite,\n             fx-caret .65s steps(1) infinite;\n}\n@keyframes fx-type {\n  0%, 8%    { width: 0; }\n  55%, 100% { width: 10ch; }\n}\n@keyframes fx-caret {\n  50% { border-color: transparent; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"TYPEWRITER\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "neon": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Neon-Haus\" text effect applied to the word \"NEON\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-neon\">NEON</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 04 Neon-Haus \u2014 buzzing neon sign with unstable tube flicker */\n.fx-neon {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .14em;\n  color: var(--ink);\n  animation: fx-neon 3.2s linear infinite;\n}\n@keyframes fx-neon {\n  0%, 18%, 21%, 24%, 100% {\n    opacity: 1;\n    text-shadow:\n      0 0 4px var(--ink),\n      0 0 11px var(--ink-2),\n      0 0 24px var(--ink-2),\n      0 0 48px var(--ink-2);\n  }\n  19%, 22%, 62% {\n    opacity: .35;\n    text-shadow: none;\n  }\n  63%, 64.5% {\n    opacity: .6;\n    text-shadow: 0 0 4px var(--ink-2);\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"NEON\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "liquid": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Aqua-Fill\" text effect applied to the word \"LIQUID\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-liquid\">LIQUID</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 05 Aqua-Fill \u2014 the word fills with liquid that rises and drains */\n.fx-liquid {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: transparent;\n  -webkit-text-stroke: 1px color-mix(in srgb, var(--ink-3) 65%, transparent);\n  background: linear-gradient(0deg,\n      var(--ink-3) 0 46%,\n      color-mix(in srgb, var(--ink-3) 55%, transparent) 48%,\n      transparent 52% 100%\n    ) 0 0 / 100% 220%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-liquid 3.4s ease-in-out infinite;\n}\n@keyframes fx-liquid {\n  0%, 100% { background-position: 0 -110%; }\n  50%      { background-position: 0 10%; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"LIQUID\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "chrome": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Chromia\" text effect applied to the word \"CHROME\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-chrome\">CHROME</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 06 Chromia \u2014 brushed-metal letters with a passing sheen */\n.fx-chrome {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  background: linear-gradient(105deg,\n      color-mix(in srgb, var(--ink) 40%, transparent) 0 38%,\n      var(--ink) 47%,\n      #fff 50%,\n      var(--ink) 53%,\n      color-mix(in srgb, var(--ink) 40%, transparent) 62% 100%\n    ) 0 0 / 260% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-chrome 2.8s ease-in-out infinite;\n}\n@keyframes fx-chrome {\n  0%   { background-position: 130% 0; }\n  60%, 100% { background-position: -130% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"CHROME\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "focus": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Lens-Drift\" text effect applied to the word \"FOCUS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-focus\" role=\"img\" aria-label=\"FOCUS\"><b aria-hidden=\"true\" style=\"--i:0\">F</b><b aria-hidden=\"true\" style=\"--i:1\">O</b><b aria-hidden=\"true\" style=\"--i:2\">C</b><b aria-hidden=\"true\" style=\"--i:3\">U</b><b aria-hidden=\"true\" style=\"--i:4\">S</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 07 Lens-Drift \u2014 letters slip in and out of focus, one after another */\n.fx-focus {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n}\n.fx-focus b {\n  font-weight: inherit;\n  display: inline-block;\n  animation: fx-focus 2.6s ease-in-out infinite;\n  animation-delay: calc(var(--i) * .18s);\n}\n@keyframes fx-focus {\n  0%, 100% { filter: blur(0);   opacity: 1; }\n  50%      { filter: blur(7px); opacity: .35; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"FOCUS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "wave": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Tidal-Type\" text effect applied to the word \"WAVE\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-wave\" role=\"img\" aria-label=\"WAVE\"><b aria-hidden=\"true\" style=\"--i:0\">W</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">V</b><b aria-hidden=\"true\" style=\"--i:3\">E</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 08 Tidal-Type \u2014 letters ride a rolling sine wave */\n.fx-wave {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink-3);\n}\n.fx-wave b {\n  font-weight: inherit;\n  display: inline-block;\n  animation: fx-wave 1.3s ease-in-out infinite;\n  animation-delay: calc(var(--i) * -.14s);\n}\n@keyframes fx-wave {\n  0%, 100% { transform: translateY(7px); }\n  50%      { transform: translateY(-7px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"WAVE\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "sliced": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Bisect\" text effect applied to the word \"SLICED\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-sliced\" data-text=\"SLICED\">SLICED</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 09 Bisect \u2014 the word is sliced horizontally; halves drift apart and re-align */\n.fx-sliced {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: transparent;\n}\n.fx-sliced::before, .fx-sliced::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: var(--ink);\n}\n.fx-sliced::before {\n  clip-path: inset(0 0 50% 0);\n  animation: fx-sliced-top 3s ease-in-out infinite;\n}\n.fx-sliced::after {\n  clip-path: inset(50% 0 0 0);\n  animation: fx-sliced-bot 3s ease-in-out infinite;\n}\n@keyframes fx-sliced-top {\n  0%, 22%, 78%, 100% { transform: translateX(0); }\n  38%, 62% { transform: translateX(8px); color: var(--ink-2); }\n}\n@keyframes fx-sliced-bot {\n  0%, 22%, 78%, 100% { transform: translateX(0); }\n  38%, 62% { transform: translateX(-8px); color: var(--ink-3); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SLICED\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "decoder": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Cipher\" text effect applied to the word \"DECODER\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-decoder\" role=\"img\" aria-label=\"DECODER\"></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 10 Cipher \u2014 scrambled characters decode into the word, then re-encrypt */\n.fx-decoder {\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink-3);\n}\n.fx-decoder::after {\n  content: \"\u2593K#R1&Q\";\n  animation: fx-decoder 4s steps(1) infinite;\n}\n@keyframes fx-decoder {\n  0%   { content: \"\u2593K#R1&Q\"; color: var(--ink-3); }\n  8%   { content: \"D%#OD3\u2592\"; }\n  16%  { content: \"DE\u2630O*ER\"; }\n  24%  { content: \"DEC0D\u00a3R\"; }\n  32%  { content: \"DECOD\u20acR\"; }\n  40%, 82% { content: \"DECODER\"; color: var(--ink); }\n  88%  { content: \"D3C0*\u20acR\"; color: var(--ink-3); }\n  94%  { content: \"\u2592#COD1Q\"; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"DECODER\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "scanner": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Redactor\" text effect applied to the word \"SCANNER\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-scanner\">SCANNER</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 11 Redactor \u2014 a scanning bar sweeps the word, lighting letters as it passes */\n.fx-scanner {\n  position: relative;\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  background: linear-gradient(90deg,\n      color-mix(in srgb, var(--ink) 25%, transparent) 0 40%,\n      var(--ink) 50%,\n      color-mix(in srgb, var(--ink) 25%, transparent) 60% 100%\n    ) 0 0 / 300% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-scan-glow 2.6s ease-in-out infinite alternate;\n}\n.fx-scanner::after {\n  content: \"\";\n  position: absolute;\n  top: -6px;\n  bottom: -6px;\n  left: 0;\n  width: 3px;\n  background: var(--ink-2);\n  box-shadow: 0 0 14px var(--ink-2);\n  animation: fx-scan-bar 2.6s ease-in-out infinite alternate;\n}\n@keyframes fx-scan-glow {\n  0%   { background-position: 100% 0; }\n  100% { background-position: 0% 0; }\n}\n@keyframes fx-scan-bar {\n  0%   { left: 0; transform: translateX(0); }\n  100% { left: 100%; transform: translateX(-100%); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SCANNER\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "ember": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Emberglow\" text effect applied to the word \"EMBER\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-ember\">EMBER</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 12 Emberglow \u2014 letters smoulder under rising fire light */\n.fx-ember {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: #ffd9a0;\n  animation: fx-ember 1.7s ease-in-out infinite alternate;\n}\n@keyframes fx-ember {\n  0% {\n    text-shadow:\n      0 -2px 6px  #ffab40,\n      0 -6px 14px #ff6d00,\n      0 -12px 28px #dd2c00,\n      0 -20px 44px rgba(213, 0, 0, .55);\n  }\n  100% {\n    text-shadow:\n      0 -3px 8px  #ffc46b,\n      0 -9px 20px #ff8f1f,\n      0 -18px 38px #ff3d00,\n      0 -30px 60px rgba(213, 0, 0, .8);\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"EMBER\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "echo": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Echo-Verse\" text effect applied to the word \"ECHO\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-echo\" data-text=\"ECHO\">ECHO</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 13 Echo-Verse \u2014 ghost copies of the word ripple outward */\n.fx-echo {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink);\n}\n.fx-echo::before, .fx-echo::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  animation: fx-echo 2s ease-out infinite;\n}\n.fx-echo::before { color: var(--ink-2); }\n.fx-echo::after  { color: var(--ink-3); animation-delay: 1s; }\n@keyframes fx-echo {\n  0%   { opacity: .8; transform: scale(1);   filter: blur(0); }\n  100% { opacity: 0;  transform: scale(1.9); filter: blur(3px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"ECHO\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "extrude": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Deep-Type\" text effect applied to the word \"DEPTH\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-extrude\">DEPTH</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 14 Deep-Type \u2014 extruded 3D block letters rocking on their axis */\n.fx-extrude {\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  text-shadow:\n    1px 1px 0 color-mix(in srgb, var(--ink-2) 80%, #000),\n    2px 2px 0 color-mix(in srgb, var(--ink-2) 70%, #000),\n    3px 3px 0 color-mix(in srgb, var(--ink-2) 60%, #000),\n    4px 4px 0 color-mix(in srgb, var(--ink-2) 50%, #000),\n    5px 5px 0 color-mix(in srgb, var(--ink-2) 40%, #000),\n    6px 6px 0 color-mix(in srgb, var(--ink-2) 30%, #000),\n    7px 7px 12px rgba(0, 0, 0, .5);\n  animation: fx-extrude 3s ease-in-out infinite;\n}\n@keyframes fx-extrude {\n  0%, 100% { transform: rotate(-3.5deg) translateY(2px); }\n  50%      { transform: rotate(3.5deg)  translateY(-4px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"DEPTH\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "contour": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Wireframe\" text effect applied to the word \"OUTLINE\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<svg class=\"fx-contour\" viewBox=\"0 0 260 56\" role=\"img\" aria-label=\"OUTLINE\"><text x=\"50%\" y=\"50%\" dominant-baseline=\"central\" text-anchor=\"middle\">OUTLINE</text></svg>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 15 Wireframe \u2014 a dashed outline endlessly traces the letterforms */\n.fx-contour {\n  width: 240px;\n  height: 56px;\n  overflow: visible;\n}\n.fx-contour text {\n  font: 600 40px \"JetBrains Mono\", monospace;\n  letter-spacing: .02em;\n  fill: color-mix(in srgb, var(--ink) 10%, transparent);\n  stroke: var(--ink-3);\n  stroke-width: 1.2;\n  stroke-dasharray: 34 66;\n  animation: fx-contour 3.2s linear infinite;\n}\n@keyframes fx-contour {\n  to { stroke-dashoffset: -100; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"OUTLINE\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "spectrum": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Prisma\" text effect applied to the word \"SPECTRUM\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-spectrum\" role=\"img\" aria-label=\"SPECTRUM\"><b aria-hidden=\"true\" style=\"--i:0\">S</b><b aria-hidden=\"true\" style=\"--i:1\">P</b><b aria-hidden=\"true\" style=\"--i:2\">E</b><b aria-hidden=\"true\" style=\"--i:3\">C</b><b aria-hidden=\"true\" style=\"--i:4\">T</b><b aria-hidden=\"true\" style=\"--i:5\">R</b><b aria-hidden=\"true\" style=\"--i:6\">U</b><b aria-hidden=\"true\" style=\"--i:7\">M</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 16 Prisma \u2014 each letter refracts a different band of the spectrum */\n.fx-spectrum {\n  font: 600 26px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n}\n.fx-spectrum b {\n  font-weight: inherit;\n  display: inline-block;\n  color: #ff2d78;\n  filter: hue-rotate(calc(var(--i) * 42deg));\n  animation: fx-spectrum 2.4s linear infinite;\n  animation-delay: calc(var(--i) * -.1s);\n}\n@keyframes fx-spectrum {\n  to { filter: hue-rotate(calc(var(--i) * 42deg + 360deg)); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SPECTRUM\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "jitter": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Jitterbug\" text effect applied to the word \"JITTER\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-jitter\">JITTER</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 17 Jitterbug \u2014 nervous analogue jitter: skews, jumps and dropped frames */\n.fx-jitter {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n  animation: fx-jitter 1.9s steps(1) infinite;\n}\n@keyframes fx-jitter {\n  0%, 7%, 15%, 55%, 100% { transform: none; opacity: 1; }\n  8%  { transform: skewX(-14deg) translateX(-4px); }\n  10% { transform: skewX(10deg) translateX(3px); }\n  12% { transform: translateY(-3px); opacity: .6; }\n  46% { transform: skewX(8deg); }\n  48% { transform: skewX(-6deg) translateY(2px); opacity: .75; }\n  50% { transform: translateX(2px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"JITTER\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "anaglyph": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Anaglyph-3D\" text effect applied to the word \"DEPTH-3D\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-anaglyph\">DEPTH-3D</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 18 Anaglyph-3D \u2014 red/cyan stereo channels drift apart and snap back */\n.fx-anaglyph {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  animation: fx-anaglyph 2.4s ease-in-out infinite;\n}\n@keyframes fx-anaglyph {\n  0%, 100% { text-shadow: 0 0 0 #ff3355, 0 0 0 #33ddff; }\n  50% {\n    text-shadow:\n      -6px 0 1px #ff3355,\n       6px 0 1px #33ddff;\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"DEPTH-3D\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "flap": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Split-Flap\" text effect applied to the word \"DEPART\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-flap\" role=\"img\" aria-label=\"DEPART\"><b aria-hidden=\"true\" style=\"--i:0\">D</b><b aria-hidden=\"true\" style=\"--i:1\">E</b><b aria-hidden=\"true\" style=\"--i:2\">P</b><b aria-hidden=\"true\" style=\"--i:3\">A</b><b aria-hidden=\"true\" style=\"--i:4\">R</b><b aria-hidden=\"true\" style=\"--i:5\">T</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 19 Split-Flap \u2014 airport departure board; letters flip on their plates */\n.fx-flap {\n  font: 600 24px/1 \"JetBrains Mono\", monospace;\n  letter-spacing: 0;\n  display: inline-flex;\n  gap: 3px;\n  perspective: 400px;\n  color: var(--ink);\n}\n.fx-flap b {\n  font-weight: inherit;\n  display: grid;\n  place-items: center;\n  width: 1.5em;\n  height: 1.9em;\n  background: linear-gradient(\n      color-mix(in srgb, var(--ink) 16%, transparent) 0 49%,\n      color-mix(in srgb, var(--ink) 8%, transparent) 51% 100%);\n  border-radius: 4px;\n  transform-origin: center;\n  animation: fx-flap 3.2s ease-in-out infinite;\n  animation-delay: calc(var(--i) * .12s);\n}\n@keyframes fx-flap {\n  0%, 55%, 100% { transform: rotateX(0); }\n  62% { transform: rotateX(-88deg); }\n  69% { transform: rotateX(0); }\n  74% { transform: rotateX(-25deg); }\n  79% { transform: rotateX(0); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"DEPART\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "crt": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Phosphor\" text effect applied to the word \"CRT_MODE\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-crt\">CRT_MODE</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 20 Phosphor \u2014 CRT terminal: green glow, rolling scanlines, tube flicker */\n.fx-crt {\n  position: relative;\n  font: 600 26px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  padding: .3em .5em;\n  color: #4dff88;\n  text-shadow: 0 0 7px rgba(77, 255, 136, .8);\n  background: rgba(20, 60, 35, .18);\n  border-radius: 4px;\n  animation: fx-crt-flicker 3.4s steps(1) infinite;\n}\n.fx-crt::after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  pointer-events: none;\n  background: repeating-linear-gradient(0deg,\n      transparent 0 2px,\n      rgba(0, 0, 0, .38) 2px 4px);\n  animation: fx-crt-roll 6s linear infinite;\n}\n@keyframes fx-crt-flicker {\n  0%, 6%, 100% { opacity: 1; }\n  3%   { opacity: .82; }\n  4.5% { opacity: .95; transform: translateY(1px); }\n  70%  { opacity: .9; }\n  71%  { opacity: 1; transform: none; }\n}\n@keyframes fx-crt-roll {\n  to { background-position: 0 48px; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"CRT_MODE\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "pop": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Pop-Riot\" text effect applied to the word \"POP!\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-pop\" role=\"img\" aria-label=\"POP!\"><b aria-hidden=\"true\" style=\"--i:0\">P</b><b aria-hidden=\"true\" style=\"--i:1\">O</b><b aria-hidden=\"true\" style=\"--i:2\">P</b><b aria-hidden=\"true\" style=\"--i:3\">!</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 21 Pop-Riot \u2014 letters leap with a hard comic-book drop shadow */\n.fx-pop {\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .04em;\n  color: var(--ink);\n}\n.fx-pop b {\n  font-weight: inherit;\n  display: inline-block;\n  animation: fx-pop 1.5s cubic-bezier(.3, 1.6, .4, 1) infinite;\n  animation-delay: calc(var(--i) * .09s);\n}\n@keyframes fx-pop {\n  0%, 55%, 100% { transform: translateY(0); text-shadow: 0 0 0 var(--ink-2); }\n  25% { transform: translateY(-12px) rotate(-4deg); text-shadow: 0 12px 0 var(--ink-2); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"POP!\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "spotlight": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Limelight\" text effect applied to the word \"SPOTLIGHT\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-spotlight\">SPOTLIGHT</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 22 Limelight \u2014 a spotlight sweeps across otherwise unlit letters */\n.fx-spotlight {\n  font: 600 26px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  background: radial-gradient(3.5ch 100% at 50% 50%,\n      var(--ink) 20%,\n      color-mix(in srgb, var(--ink) 14%, transparent) 75%\n    ) 0 0 / 300% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-spotlight 3.4s ease-in-out infinite alternate;\n}\n@keyframes fx-spotlight {\n  0%   { background-position: 100% 0; }\n  100% { background-position: 0% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SPOTLIGHT\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "elastic": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Rubber-Band\" text effect applied to the word \"BOING\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-elastic\">BOING</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 23 Rubber-Band \u2014 the word squashes and stretches like an elastic band */\n.fx-elastic {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink-2);\n  animation: fx-elastic 1.6s ease-in-out infinite;\n}\n@keyframes fx-elastic {\n  0%, 100% { transform: scale(1, 1); }\n  28% { transform: scale(1.28, .72); }\n  44% { transform: scale(.78, 1.24); }\n  60% { transform: scale(1.12, .9); }\n  74% { transform: scale(.96, 1.05); }\n  86% { transform: scale(1.02, .98); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"BOING\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "mirror": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Still-Water\" text effect applied to the word \"MIRROR\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-mirror\">MIRROR</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 24 Still-Water \u2014 the word floats over its own rippling reflection\n   (uses -webkit-box-reflect: Chromium & Safari; degrades gracefully) */\n.fx-mirror {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  -webkit-box-reflect: below 2px linear-gradient(transparent 45%, rgba(255, 255, 255, .28));\n  animation: fx-mirror 3s ease-in-out infinite;\n}\n@keyframes fx-mirror {\n  0%, 100% { transform: translateY(0); }\n  50%      { transform: translateY(-6px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"MIRROR\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "ransom": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Ransom-Note\" text effect applied to the word \"RANSOM\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-ransom\" role=\"img\" aria-label=\"RANSOM\"><b aria-hidden=\"true\" style=\"--i:0\">R</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">N</b><b aria-hidden=\"true\" style=\"--i:3\">S</b><b aria-hidden=\"true\" style=\"--i:4\">O</b><b aria-hidden=\"true\" style=\"--i:5\">M</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 25 Ransom-Note \u2014 mismatched cut-out letters shifting on the page */\n.fx-ransom {\n  font: 600 24px/1 \"JetBrains Mono\", monospace;\n  letter-spacing: 0;\n  display: inline-flex;\n  gap: 4px;\n  color: var(--ink);\n}\n.fx-ransom b {\n  font-weight: inherit;\n  display: grid;\n  place-items: center;\n  padding: .28em .3em;\n  border-radius: 3px;\n  background: color-mix(in srgb, var(--ink) 14%, transparent);\n  animation: fx-ransom 2.8s ease-in-out infinite alternate;\n  animation-delay: calc(var(--i) * -.5s);\n}\n.fx-ransom b:nth-child(odd) {\n  background: var(--ink-2);\n  color: #14020f;\n}\n.fx-ransom b:nth-child(3n) {\n  background: transparent;\n  outline: 1.5px dashed color-mix(in srgb, var(--ink) 55%, transparent);\n  color: var(--ink-3);\n}\n@keyframes fx-ransom {\n  0%   { transform: rotate(-6deg) translateY(1.5px); }\n  100% { transform: rotate(6deg)  translateY(-1.5px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"RANSOM\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "melt": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Meltdown\" text effect applied to the word \"MELT\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-melt\" role=\"img\" aria-label=\"MELT\"><b aria-hidden=\"true\" style=\"--i:0\">M</b><b aria-hidden=\"true\" style=\"--i:1\">E</b><b aria-hidden=\"true\" style=\"--i:2\">L</b><b aria-hidden=\"true\" style=\"--i:3\">T</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 26 Meltdown \u2014 letters sag, stretch and drip before snapping back */\n.fx-melt {\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink-2);\n}\n.fx-melt b {\n  font-weight: inherit;\n  display: inline-block;\n  transform-origin: top center;\n  animation: fx-melt 2.8s ease-in infinite;\n  animation-delay: calc(var(--i) * .22s);\n}\n@keyframes fx-melt {\n  0%, 45%  { transform: scaleY(1) translateY(0); filter: blur(0); }\n  70%      { transform: scaleY(1.55) translateY(7px) scaleX(.92); filter: blur(1.5px); }\n  85%, 100% { transform: scaleY(1) translateY(0); filter: blur(0); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"MELT\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "heartbeat": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Cardio\" text effect applied to the word \"PULSE\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-heartbeat\">PULSE</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 27 Cardio \u2014 a lub-dub heartbeat with a systolic glow */\n.fx-heartbeat {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink-2);\n  animation: fx-heartbeat 1.4s ease-in-out infinite;\n}\n@keyframes fx-heartbeat {\n  0%, 100% { transform: scale(1); text-shadow: 0 0 0 transparent; }\n  14% { transform: scale(1.14); text-shadow: 0 0 18px color-mix(in srgb, var(--ink-2) 65%, transparent); }\n  28% { transform: scale(1); }\n  42% { transform: scale(1.18); text-shadow: 0 0 26px color-mix(in srgb, var(--ink-2) 80%, transparent); }\n  70% { transform: scale(1); text-shadow: 0 0 0 transparent; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"PULSE\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "marker": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Hi-Liter\" text effect applied to the word \"MARKED\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-marker\">MARKED</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 28 Hi-Liter \u2014 a marker pen paints across the word, then lifts off */\n.fx-marker {\n  font: 600 28px/1.3 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  padding: .1em .2em;\n  background: linear-gradient(100deg,\n      color-mix(in srgb, var(--ink-2) 85%, transparent),\n      color-mix(in srgb, var(--ink-2) 60%, transparent)\n    ) no-repeat 0 62%;\n  background-size: 0% 46%;\n  animation: fx-marker 3s ease-in-out infinite;\n}\n@keyframes fx-marker {\n  0%        { background-size: 0% 46%;   background-position: 0 62%; }\n  38%, 62%  { background-size: 100% 46%; background-position: 0 62%; }\n  100%      { background-size: 0% 46%;   background-position: 100% 62%; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"MARKED\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "sundial": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Sundial\" text effect applied to the word \"SHADOW\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-sundial\">SHADOW</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 29 Sundial \u2014 a long cast shadow circles the letters like a passing sun */\n.fx-sundial {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  animation: fx-sundial 5s linear infinite;\n}\n@keyframes fx-sundial {\n  0%, 100% {\n    text-shadow:\n      4px 0 var(--ink-2),\n      8px 0 color-mix(in srgb, var(--ink-2) 55%, transparent),\n      12px 0 color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  12.5% {\n    text-shadow:\n      2.8px 2.8px var(--ink-2),\n      5.6px 5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      8.4px 8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  25% {\n    text-shadow:\n      0 4px var(--ink-2),\n      0 8px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      0 12px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  37.5% {\n    text-shadow:\n      -2.8px 2.8px var(--ink-2),\n      -5.6px 5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      -8.4px 8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  50% {\n    text-shadow:\n      -4px 0 var(--ink-2),\n      -8px 0 color-mix(in srgb, var(--ink-2) 55%, transparent),\n      -12px 0 color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  62.5% {\n    text-shadow:\n      -2.8px -2.8px var(--ink-2),\n      -5.6px -5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      -8.4px -8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  75% {\n    text-shadow:\n      0 -4px var(--ink-2),\n      0 -8px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      0 -12px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  87.5% {\n    text-shadow:\n      2.8px -2.8px var(--ink-2),\n      5.6px -5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      8.4px -8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SHADOW\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "negative": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Negativ\" text effect applied to the word \"INVERTED\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-negative\">INVERTED</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 30 Negative \u2014 an inversion bar sweeps the word, flipping letters to their\n   photographic negative via mix-blend-mode: difference */\n.fx-negative {\n  position: relative;\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  padding: .18em .22em;\n}\n.fx-negative::after {\n  content: \"\";\n  position: absolute;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 34%;\n  background: var(--ink);\n  mix-blend-mode: difference;\n  animation: fx-negative 2.4s ease-in-out infinite alternate;\n}\n@keyframes fx-negative {\n  0%   { left: 0;    transform: translateX(0); }\n  100% { left: 100%; transform: translateX(-100%); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"INVERTED\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "hologram": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Holograph\" text effect applied to the word \"HOLOGRAM\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-hologram\" data-text=\"HOLOGRAM\">HOLOGRAM</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 31 Holograph \u2014 translucent scanline projection with drifting chroma edges */\n.fx-hologram {\n  position: relative;\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: color-mix(in srgb, var(--ink-3) 72%, var(--ink));\n  text-shadow:\n    0 0 6px color-mix(in srgb, var(--ink-3) 60%, transparent),\n    0 0 22px color-mix(in srgb, var(--ink-3) 45%, transparent);\n  isolation: isolate;\n  animation: fx-hologram-float 3.8s ease-in-out infinite;\n}\n.fx-hologram::before,\n.fx-hologram::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-hologram::before {\n  color: var(--ink-2);\n  clip-path: inset(0 0 52% 0);\n  opacity: .58;\n  animation: fx-hologram-split 2.8s steps(1) infinite;\n}\n.fx-hologram::after {\n  color: transparent;\n  background: repeating-linear-gradient(\n      0deg,\n      color-mix(in srgb, var(--ink) 70%, transparent) 0 1px,\n      transparent 1px 4px\n    );\n  -webkit-background-clip: text;\n  background-clip: text;\n  opacity: .48;\n  animation: fx-hologram-lines 1.2s linear infinite;\n}\n@keyframes fx-hologram-float {\n  0%, 100% { transform: translateY(2px) skewX(0deg); filter: brightness(1); }\n  50%      { transform: translateY(-4px) skewX(-3deg); filter: brightness(1.35); }\n}\n@keyframes fx-hologram-split {\n  0%, 14%, 40%, 72%, 100% { transform: translateX(0); opacity: .22; }\n  15% { transform: translateX(-5px); opacity: .8; }\n  17% { transform: translateX(4px);  opacity: .6; }\n  74% { transform: translateX(3px);  opacity: .7; }\n}\n@keyframes fx-hologram-lines {\n  to { background-position: 0 12px; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"HOLOGRAM\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "foil": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Gold-Foil\" text effect applied to the word \"LUXE\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-foil\">LUXE</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 32 Gold-Foil \u2014 embossed metallic letters with a diagonal specular sweep */\n.fx-foil {\n  font: 600 36px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: transparent;\n  background:\n    linear-gradient(108deg,\n      #7a4a08 0%,\n      #f2c15d 18%,\n      #fff4b8 24%,\n      #b87014 36%,\n      #f6d66d 51%,\n      #8e560b 68%,\n      #fff0a2 76%,\n      #c98218 100%\n    ) 0 0 / 230% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  text-shadow:\n    0 1px 0 rgba(255, 244, 184, .45),\n    0 9px 20px rgba(0, 0, 0, .36);\n  animation: fx-foil 3.1s ease-in-out infinite;\n}\n@keyframes fx-foil {\n  0%, 100% { background-position: 115% 0; filter: saturate(1.05); }\n  45%      { background-position: -35% 0; filter: saturate(1.35) brightness(1.12); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"LUXE\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "pixel": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Pixel-Sort\" text effect applied to the word \"PIXELS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-pixel\" data-text=\"PIXELS\">PIXELS</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 33 Pixel-Sort \u2014 blocky offsets smear the word into stepped digital columns */\n.fx-pixel {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  text-shadow:\n    1px 0 0 var(--ink-2),\n   -1px 0 0 var(--ink-3);\n}\n.fx-pixel::before,\n.fx-pixel::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  opacity: .85;\n}\n.fx-pixel::before {\n  color: var(--ink-2);\n  clip-path: polygon(0 0, 100% 0, 100% 28%, 0 28%, 0 44%, 100% 44%, 100% 58%, 0 58%);\n  animation: fx-pixel-a 2.2s steps(1) infinite;\n}\n.fx-pixel::after {\n  color: var(--ink-3);\n  clip-path: polygon(0 30%, 100% 30%, 100% 42%, 0 42%, 0 66%, 100% 66%, 100% 82%, 0 82%);\n  animation: fx-pixel-b 2.2s steps(1) infinite;\n}\n@keyframes fx-pixel-a {\n  0%, 20%, 54%, 100% { transform: translate(0); }\n  22% { transform: translate(7px, -2px); }\n  24% { transform: translate(-4px, 1px); }\n  56% { transform: translate(5px, 2px); }\n  58% { transform: translate(-7px, 0); }\n}\n@keyframes fx-pixel-b {\n  0%, 18%, 58%, 100% { transform: translate(0); }\n  19% { transform: translate(-6px, 2px); }\n  21% { transform: translate(4px, -1px); }\n  60% { transform: translate(-5px, -2px); }\n  63% { transform: translate(6px, 1px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"PIXELS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "starlight": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Starlight\" text effect applied to the word \"STARDUST\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-starlight\">STARDUST</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 34 Starlight \u2014 a constellation shimmer twinkles across frosted letter faces */\n.fx-starlight {\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background:\n    radial-gradient(circle at 14% 35%, #fff 0 2px, transparent 2.4px),\n    radial-gradient(circle at 42% 68%, #fff 0 1.6px, transparent 2px),\n    radial-gradient(circle at 75% 24%, #fff 0 2.2px, transparent 2.6px),\n    linear-gradient(100deg, var(--ink), var(--ink-3), var(--ink));\n  background-size: 120px 60px, 150px 72px, 180px 68px, 240% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  filter: drop-shadow(0 0 10px color-mix(in srgb, var(--ink-3) 45%, transparent));\n  animation: fx-starlight 3.6s linear infinite;\n}\n@keyframes fx-starlight {\n  0%   { background-position: 0 0, 0 0, 0 0, 120% 0; }\n  100% { background-position: 120px 0, -150px 0, 180px 0, -120% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"STARDUST\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "blueprint": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Blueprint\" text effect applied to the word \"DRAFT\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-blueprint\">DRAFT</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 35 Blueprint \u2014 technical outline letters over a moving drafting grid */\n.fx-blueprint {\n  font: 600 28px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: transparent;\n  padding: .28em .45em;\n  -webkit-text-stroke: 1px color-mix(in srgb, var(--ink-3) 80%, var(--ink));\n  background:\n    linear-gradient(90deg, color-mix(in srgb, var(--ink-3) 22%, transparent) 1px, transparent 1px),\n    linear-gradient(0deg, color-mix(in srgb, var(--ink-3) 18%, transparent) 1px, transparent 1px),\n    linear-gradient(90deg, color-mix(in srgb, var(--ink-3) 42%, transparent), transparent 35% 65%, color-mix(in srgb, var(--ink-3) 42%, transparent));\n  background-size: 12px 12px, 12px 12px, 240% 100%;\n  background-position: 0 0, 0 0, 100% 0;\n  border: 1px solid color-mix(in srgb, var(--ink-3) 42%, transparent);\n  box-shadow: inset 0 0 18px color-mix(in srgb, var(--ink-3) 18%, transparent);\n  animation: fx-blueprint 4s linear infinite;\n}\n@keyframes fx-blueprint {\n  to { background-position: 24px 0, 0 24px, -100% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"DRAFT\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "vapor": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Vapor-Trail\" text effect applied to the word \"VAPOR\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-vapor\" data-text=\"VAPOR\">VAPOR</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 36 Vapor-Trail \u2014 soft neon copies trail behind a drifting word */\n.fx-vapor {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .16em;\n  color: var(--ink);\n  text-shadow: 0 0 12px color-mix(in srgb, var(--ink) 45%, transparent);\n  animation: fx-vapor-main 2.8s ease-in-out infinite;\n}\n.fx-vapor::before,\n.fx-vapor::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  filter: blur(2px);\n  opacity: .62;\n}\n.fx-vapor::before {\n  color: var(--ink-2);\n  animation: fx-vapor-a 2.8s ease-in-out infinite;\n}\n.fx-vapor::after {\n  color: var(--ink-3);\n  animation: fx-vapor-b 2.8s ease-in-out infinite;\n}\n@keyframes fx-vapor-main {\n  0%, 100% { transform: translateX(-3px); }\n  50%      { transform: translateX(3px); }\n}\n@keyframes fx-vapor-a {\n  0%, 100% { transform: translate(-10px, 6px) scaleX(1.08); opacity: .22; }\n  50%      { transform: translate(-18px, -4px) scaleX(1.22); opacity: .58; }\n}\n@keyframes fx-vapor-b {\n  0%, 100% { transform: translate(10px, -5px) scaleX(1.08); opacity: .2; }\n  50%      { transform: translate(18px, 5px) scaleX(1.2); opacity: .56; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"VAPOR\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "kinetic": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Kinetic-Type\" text effect applied to the word \"KINETIC\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-kinetic\" role=\"img\" aria-label=\"KINETIC\"><b aria-hidden=\"true\" style=\"--i:0\">K</b><b aria-hidden=\"true\" style=\"--i:1\">I</b><b aria-hidden=\"true\" style=\"--i:2\">N</b><b aria-hidden=\"true\" style=\"--i:3\">E</b><b aria-hidden=\"true\" style=\"--i:4\">T</b><b aria-hidden=\"true\" style=\"--i:5\">I</b><b aria-hidden=\"true\" style=\"--i:6\">C</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 37 Kinetic-Type \u2014 characters slide on staggered rails and lock into place */\n.fx-kinetic {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-kinetic b {\n  font-weight: inherit;\n  display: inline-block;\n  transform-origin: center;\n  animation: fx-kinetic 2.6s cubic-bezier(.2, .8, .2, 1) infinite;\n  animation-delay: calc(var(--i) * .08s);\n}\n.fx-kinetic b:nth-child(2n) { color: var(--ink-3); }\n.fx-kinetic b:nth-child(3n) { color: var(--ink-2); }\n@keyframes fx-kinetic {\n  0%, 18%   { transform: translateY(0) rotate(0deg); filter: blur(0); }\n  34%       { transform: translateY(-18px) rotate(-9deg); filter: blur(.4px); }\n  52%       { transform: translateY(14px) rotate(7deg); }\n  68%, 100% { transform: translateY(0) rotate(0deg); filter: blur(0); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"KINETIC\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "blackout": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Blackout\" text effect applied to the word \"CLASSIFIED\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-blackout\" data-text=\"CLASSIFIED\">CLASSIFIED</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 38 Blackout \u2014 redaction strips repeatedly hide and reveal classified text */\n.fx-blackout {\n  position: relative;\n  font: 600 24px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  padding: .28em .12em;\n}\n.fx-blackout::before,\n.fx-blackout::after {\n  content: \"\";\n  position: absolute;\n  left: -.18em;\n  right: -.18em;\n  height: 44%;\n  background: var(--ink);\n  box-shadow: 0 0 0 1px color-mix(in srgb, var(--ink-2) 45%, transparent);\n  animation: fx-blackout 3s steps(1) infinite;\n}\n.fx-blackout::before {\n  top: 0;\n  transform-origin: left;\n}\n.fx-blackout::after {\n  bottom: 0;\n  transform-origin: right;\n  animation-delay: .28s;\n}\n@keyframes fx-blackout {\n  0%, 20%, 72%, 100% { transform: scaleX(0); }\n  26%, 58%           { transform: scaleX(1); }\n  64%                { transform: scaleX(.38); }\n}\n\n@keyframes reveal {\n  from { opacity: 0; transform: translateY(20px); clip-path: inset(0 0 100% 0); }\n  to   { opacity: 1; transform: translateY(0);    clip-path: inset(0 0 0 0); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"CLASSIFIED\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "magnetic": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Magnetic\" text effect applied to the word \"MAGNET\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-magnetic\" role=\"img\" aria-label=\"MAGNET\"><b aria-hidden=\"true\" style=\"--i:0\">M</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">G</b><b aria-hidden=\"true\" style=\"--i:3\">N</b><b aria-hidden=\"true\" style=\"--i:4\">E</b><b aria-hidden=\"true\" style=\"--i:5\">T</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 39 Magnetic \u2014 letters attract into a tight field, then repel outward */\n.fx-magnetic {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-magnetic b {\n  font-weight: inherit;\n  display: inline-block;\n  text-shadow: 0 0 0 var(--ink-2);\n  animation: fx-magnetic 2.7s ease-in-out infinite;\n  animation-delay: calc(var(--i) * .07s);\n}\n.fx-magnetic b:nth-child(odd) { color: var(--ink-3); }\n@keyframes fx-magnetic {\n  0%, 100% {\n    transform: translateX(0) scale(1);\n    text-shadow: 0 0 0 transparent;\n  }\n  42% {\n    transform: translateX(calc((3 - var(--i)) * 2px)) scale(1.12);\n    text-shadow: 0 0 16px color-mix(in srgb, var(--ink-2) 70%, transparent);\n  }\n  62% {\n    transform: translateX(calc((var(--i) - 3) * 3px)) scale(.96);\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"MAGNET\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "mesh": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Luma-Mesh\" text effect applied to the word \"MESH\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-mesh\">MESH</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 40 Luma-Mesh \u2014 animated mesh-gradient ink with a soft luminous edge */\n.fx-mesh {\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: transparent;\n  background:\n    radial-gradient(circle at 18% 28%, var(--ink-2) 0 12%, transparent 28%),\n    radial-gradient(circle at 78% 24%, var(--ink-3) 0 14%, transparent 30%),\n    radial-gradient(circle at 44% 82%, #ffffff 0 10%, transparent 24%),\n    linear-gradient(110deg, var(--ink-2), var(--ink-3));\n  background-size: 180% 180%, 170% 170%, 160% 160%, 220% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  filter: drop-shadow(0 0 18px color-mix(in srgb, var(--ink-3) 28%, transparent));\n  animation: fx-mesh 4.8s ease-in-out infinite alternate;\n}\n@keyframes fx-mesh {\n  0%   { background-position: 0% 20%, 100% 10%, 35% 100%, 100% 0; }\n  100% { background-position: 100% 80%, 0% 60%, 70% 0%, -100% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"MESH\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "iridescent": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Iridescent\" text effect applied to the word \"IRIS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-iridescent\">IRIS</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 41 Iridescent \u2014 pearlescent text whose colour shifts with a moving angle */\n.fx-iridescent {\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background: conic-gradient(\n      from 20deg,\n      #f7fbff,\n      var(--ink-3),\n      #bba7ff,\n      var(--ink-2),\n      #fff7d6,\n      #f7fbff\n    ) 50% 50% / 220% 220%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  text-shadow: 0 10px 28px rgba(0, 0, 0, .28);\n  animation: fx-iridescent 4.5s linear infinite;\n}\n@keyframes fx-iridescent {\n  to { filter: hue-rotate(360deg); background-position: 120% 50%; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"IRIS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "glass": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Glass-Type\" text effect applied to the word \"GLASS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-glass\" data-text=\"GLASS\">GLASS</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 42 Glass-Type \u2014 translucent frosted letters with an offset refraction layer */\n.fx-glass {\n  position: relative;\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: color-mix(in srgb, var(--ink) 58%, transparent);\n  -webkit-text-stroke: 1px color-mix(in srgb, var(--ink) 52%, transparent);\n  text-shadow:\n    0 1px 0 rgba(255, 255, 255, .4),\n    0 12px 24px rgba(0, 0, 0, .3);\n  animation: fx-glass 3.4s ease-in-out infinite;\n}\n.fx-glass::before {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: var(--ink-3);\n  opacity: .34;\n  filter: blur(3px);\n  transform: translate(4px, 5px);\n}\n.fx-glass::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: transparent;\n  background: linear-gradient(115deg, transparent 22%, #fff 44%, transparent 62%) 110% 0 / 260% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  opacity: .55;\n  animation: fx-glass-sheen 2.7s ease-in-out infinite;\n}\n@keyframes fx-glass {\n  50% { transform: translateY(-3px); }\n}\n@keyframes fx-glass-sheen {\n  to { background-position: -110% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"GLASS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "datastream": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Datastream\" text effect applied to the word \"STREAM\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-datastream\">STREAM</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 43 Datastream \u2014 matrix-like data bands flow through clipped letterforms */\n.fx-datastream {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background:\n    repeating-linear-gradient(0deg,\n      var(--ink-3) 0 2px,\n      color-mix(in srgb, var(--ink-3) 35%, transparent) 2px 4px,\n      transparent 4px 8px\n    ),\n    linear-gradient(90deg, var(--ink-2), var(--ink-3), var(--ink));\n  background-size: 100% 32px, 240% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-datastream 1.6s linear infinite;\n}\n.fx-datastream::after {\n  content: \"0101 1100 0011\";\n  position: absolute;\n  left: 50%;\n  top: 100%;\n  width: 170%;\n  color: color-mix(in srgb, var(--ink-3) 42%, transparent);\n  font-size: .42em;\n  letter-spacing: .16em;\n  transform: translateX(-50%);\n  animation: fx-datastream-code 1.6s linear infinite;\n}\n@keyframes fx-datastream {\n  to { background-position: 0 32px, -120% 0; }\n}\n@keyframes fx-datastream-code {\n  0% { opacity: .2; transform: translate(-50%, -2px); }\n  50% { opacity: .7; }\n  100% { opacity: .2; transform: translate(-50%, 10px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"STREAM\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "orbit": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Orbitals\" text effect applied to the word \"ORBIT\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-orbit\" role=\"img\" aria-label=\"ORBIT\"><b aria-hidden=\"true\" style=\"--i:0\">O</b><b aria-hidden=\"true\" style=\"--i:1\">R</b><b aria-hidden=\"true\" style=\"--i:2\">B</b><b aria-hidden=\"true\" style=\"--i:3\">I</b><b aria-hidden=\"true\" style=\"--i:4\">T</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 44 Orbitals \u2014 letters circle tiny invisible tracks before settling back */\n.fx-orbit {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-orbit b {\n  font-weight: inherit;\n  display: inline-block;\n  transform-origin: 50% 120%;\n  animation: fx-orbit 2.8s cubic-bezier(.45, 0, .2, 1) infinite;\n  animation-delay: calc(var(--i) * -.18s);\n}\n.fx-orbit b:nth-child(odd) { color: var(--ink-3); }\n.fx-orbit b:nth-child(3n) { color: var(--ink-2); }\n@keyframes fx-orbit {\n  0%, 100% { transform: translate(0, 0) rotate(0deg); }\n  35%      { transform: translate(5px, -13px) rotate(16deg); }\n  70%      { transform: translate(-5px, 7px) rotate(-10deg); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"ORBIT\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "prismcut": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Prism-Cut\" text effect applied to the word \"PRISM\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-prismcut\" data-text=\"PRISM\">PRISM</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 45 Prism-Cut \u2014 diagonal glass shards refract the word into colour offsets */\n.fx-prismcut {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink);\n}\n.fx-prismcut::before,\n.fx-prismcut::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-prismcut::before {\n  color: var(--ink-2);\n  clip-path: polygon(0 0, 66% 0, 46% 100%, 0 100%);\n  animation: fx-prismcut-a 3s ease-in-out infinite;\n}\n.fx-prismcut::after {\n  color: var(--ink-3);\n  clip-path: polygon(42% 0, 100% 0, 100% 100%, 24% 100%);\n  animation: fx-prismcut-b 3s ease-in-out infinite;\n}\n@keyframes fx-prismcut-a {\n  0%, 100% { transform: translate(0); opacity: .35; }\n  50%      { transform: translate(-7px, -2px); opacity: .85; }\n}\n@keyframes fx-prismcut-b {\n  0%, 100% { transform: translate(0); opacity: .35; }\n  50%      { transform: translate(7px, 2px); opacity: .85; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"PRISM\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "softblur": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Soft-Blur\" text effect applied to the word \"FOCUS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-softblur\">FOCUS</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 46 Soft-Blur \u2014 a modern bloom effect that breathes between crisp and hazy */\n.fx-softblur {\n  font: 600 36px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  text-shadow:\n    0 0 0 var(--ink),\n    0 0 18px color-mix(in srgb, var(--ink-2) 45%, transparent),\n    0 0 34px color-mix(in srgb, var(--ink-3) 35%, transparent);\n  animation: fx-softblur 3.2s ease-in-out infinite;\n}\n@keyframes fx-softblur {\n  0%, 100% { filter: blur(0); opacity: 1; transform: scale(1); }\n  50%      { filter: blur(2.4px); opacity: .72; transform: scale(1.045); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"FOCUS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "laser": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Laser-Cut\" text effect applied to the word \"LASER\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-laser\" data-text=\"LASER\">LASER</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 47 Laser-Cut \u2014 a hot scanning blade carves a bright split through the word */\n.fx-laser {\n  position: relative;\n  font: 600 34px/1.08 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink);\n}\n.fx-laser::before {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: var(--ink-2);\n  clip-path: inset(44% 0 42% 0);\n  text-shadow: 0 0 10px var(--ink-2);\n  animation: fx-laser-slice 2.2s ease-in-out infinite;\n}\n.fx-laser::after {\n  content: \"\";\n  position: absolute;\n  left: -8%;\n  top: 49%;\n  width: 116%;\n  height: 2px;\n  background: linear-gradient(90deg, transparent, #fff, var(--ink-2), transparent);\n  box-shadow: 0 0 13px var(--ink-2);\n  animation: fx-laser 2.2s ease-in-out infinite;\n}\n@keyframes fx-laser {\n  0%, 100% { transform: translateY(-14px) scaleX(.35); opacity: 0; }\n  44%, 58% { transform: translateY(0) scaleX(1); opacity: 1; }\n}\n@keyframes fx-laser-slice {\n  0%, 100% { transform: translateX(0); opacity: .2; }\n  50%      { transform: translateX(5px); opacity: 1; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"LASER\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "microchip": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Microchip\" text effect applied to the word \"CIRCUIT\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-microchip\">CIRCUIT</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 48 Microchip \u2014 circuit-board traces scroll beneath monospaced letter masks */\n.fx-microchip {\n  font: 600 28px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: transparent;\n  padding: .35em .45em;\n  background:\n    linear-gradient(90deg, transparent 0 44%, var(--ink-3) 44% 47%, transparent 47% 100%),\n    linear-gradient(0deg, transparent 0 46%, var(--ink-2) 46% 49%, transparent 49% 100%),\n    repeating-linear-gradient(90deg, color-mix(in srgb, var(--ink-3) 35%, transparent) 0 1px, transparent 1px 14px),\n    linear-gradient(100deg, var(--ink), var(--ink-3));\n  background-size: 42px 42px, 42px 42px, 14px 100%, 220% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  border: 1px solid color-mix(in srgb, var(--ink-3) 24%, transparent);\n  animation: fx-microchip 3.8s linear infinite;\n}\n@keyframes fx-microchip {\n  to { background-position: 42px 0, 0 42px, 14px 0, -120% 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"CIRCUIT\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "heatmap": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Heatmap\" text effect applied to the word \"THERMAL\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-heatmap\">THERMAL</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 49 Heatmap \u2014 thermal colour blooms migrate across dense letter shapes */\n.fx-heatmap {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: transparent;\n  background:\n    radial-gradient(circle at 20% 50%, #fff26d 0 8%, transparent 24%),\n    radial-gradient(circle at 70% 40%, #ff4f8b 0 12%, transparent 28%),\n    radial-gradient(circle at 44% 78%, var(--ink-3) 0 14%, transparent 32%),\n    linear-gradient(90deg, #2d7dff, #36f0b2, #ffcf4a, #ff4f8b);\n  background-size: 130% 130%, 150% 150%, 140% 140%, 260% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-heatmap 3.5s ease-in-out infinite alternate;\n}\n@keyframes fx-heatmap {\n  0%   { background-position: 0 0, 100% 20%, 40% 100%, 100% 0; filter: saturate(1); }\n  100% { background-position: 100% 90%, 0 60%, 80% 0, -100% 0; filter: saturate(1.35); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"THERMAL\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "parallax": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Parallax\" text effect applied to the word \"LAYERS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-parallax\" role=\"img\" aria-label=\"LAYERS\"><b aria-hidden=\"true\" style=\"--i:0\">L</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">Y</b><b aria-hidden=\"true\" style=\"--i:3\">E</b><b aria-hidden=\"true\" style=\"--i:4\">R</b><b aria-hidden=\"true\" style=\"--i:5\">S</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 50 Parallax \u2014 coloured shadow layers drift at different speeds per letter */\n.fx-parallax {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-parallax b {\n  font-weight: inherit;\n  display: inline-block;\n  text-shadow:\n    4px 4px 0 color-mix(in srgb, var(--ink-2) 72%, transparent),\n   -4px -3px 0 color-mix(in srgb, var(--ink-3) 72%, transparent);\n  animation: fx-parallax 3s ease-in-out infinite;\n  animation-delay: calc(var(--i) * -.11s);\n}\n@keyframes fx-parallax {\n  0%, 100% {\n    transform: translate(0);\n    text-shadow:\n      4px 4px 0 color-mix(in srgb, var(--ink-2) 72%, transparent),\n     -4px -3px 0 color-mix(in srgb, var(--ink-3) 72%, transparent);\n  }\n  50% {\n    transform: translateY(-4px);\n    text-shadow:\n      8px 2px 0 color-mix(in srgb, var(--ink-2) 72%, transparent),\n     -8px -1px 0 color-mix(in srgb, var(--ink-3) 72%, transparent);\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"LAYERS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "inktrap": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Ink-Trap\" text effect applied to the word \"INK\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-inktrap\" data-text=\"INK\">INK</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 51 Ink-Trap \u2014 editorial serif-like traps pulse as heavy ink floods corners */\n.fx-inktrap {\n  position: relative;\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .04em;\n  color: var(--ink);\n}\n.fx-inktrap::before,\n.fx-inktrap::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-inktrap::before {\n  color: transparent;\n  -webkit-text-stroke: 1px var(--ink-3);\n  opacity: .65;\n  animation: fx-inktrap-line 2.7s ease-in-out infinite;\n}\n.fx-inktrap::after {\n  color: var(--ink-2);\n  clip-path: polygon(0 0, 100% 0, 84% 22%, 18% 22%, 0 0, 0 100%, 20% 78%, 86% 78%, 100% 100%, 0 100%);\n  mix-blend-mode: screen;\n  animation: fx-inktrap-fill 2.7s ease-in-out infinite;\n}\n@keyframes fx-inktrap-line {\n  50% { transform: scale(1.05); opacity: .95; }\n}\n@keyframes fx-inktrap-fill {\n  0%, 100% { opacity: .18; transform: scale(.98); }\n  50%      { opacity: .72; transform: scale(1.02); }\n}\n\n@keyframes pulse {\n  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 color-mix(in srgb, var(--primary) 50%, transparent); }\n  50% { opacity: .5; box-shadow: 0 0 0 6px transparent; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"INK\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "topographic": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Topographic\" text effect applied to the word \"TERRAIN\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-topographic\">TERRAIN</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 52 Topographic \u2014 contour-map lines crawl through the text fill */\n.fx-topographic {\n  font: 600 28px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: transparent;\n  background:\n    repeating-radial-gradient(ellipse at 30% 55%,\n      var(--ink) 0 1px,\n      transparent 1px 7px\n    ),\n    repeating-radial-gradient(ellipse at 78% 35%,\n      var(--ink-3) 0 1px,\n      transparent 1px 8px\n    );\n  background-size: 130px 70px, 170px 80px;\n  -webkit-background-clip: text;\n  background-clip: text;\n  filter: drop-shadow(0 0 8px color-mix(in srgb, var(--ink-3) 25%, transparent));\n  animation: fx-topographic 5s linear infinite;\n}\n@keyframes fx-topographic {\n  to { background-position: 130px 0, -170px 0; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"TERRAIN\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "signal": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Signal-Noise\" text effect applied to the word \"SIGNAL\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-signal\" data-text=\"SIGNAL\">SIGNAL</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 53 Signal-Noise \u2014 broadcast interference bends the word into noisy bands */\n.fx-signal {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  animation: fx-signal 2s steps(1) infinite;\n}\n.fx-signal::before,\n.fx-signal::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-signal::before {\n  color: var(--ink-2);\n  clip-path: inset(0 0 58% 0);\n  animation: fx-signal-a 2s steps(1) infinite;\n}\n.fx-signal::after {\n  color: var(--ink-3);\n  clip-path: inset(42% 0 0 0);\n  animation: fx-signal-b 2s steps(1) infinite;\n}\n@keyframes fx-signal {\n  0%, 21%, 47%, 100% { filter: none; }\n  22%, 24% { filter: blur(.8px) contrast(1.4); }\n}\n@keyframes fx-signal-a {\n  0%, 20%, 52%, 100% { transform: translate(0); opacity: .15; }\n  23% { transform: translate(-8px, -1px); opacity: .9; }\n  54% { transform: translate(5px, 2px); opacity: .7; }\n}\n@keyframes fx-signal-b {\n  0%, 24%, 58%, 100% { transform: translate(0); opacity: .12; }\n  27% { transform: translate(8px, 1px); opacity: .86; }\n  60% { transform: translate(-5px, -2px); opacity: .72; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SIGNAL\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "portal": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Portal\" text effect applied to the word \"PORTAL\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-portal\" data-text=\"PORTAL\">PORTAL</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 54 Portal \u2014 a conic vortex opens inside the letters and rotates behind them */\n.fx-portal {\n  position: relative;\n  font: 600 34px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background: conic-gradient(from 0deg, var(--ink-2), var(--ink), var(--ink-3), var(--ink-2));\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-portal 3.6s linear infinite;\n}\n.fx-portal::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: transparent;\n  background: radial-gradient(circle, transparent 0 30%, #fff 34% 36%, transparent 40% 100%);\n  -webkit-background-clip: text;\n  background-clip: text;\n  opacity: .72;\n  animation: fx-portal-pulse 1.8s ease-in-out infinite;\n}\n@keyframes fx-portal {\n  to { filter: hue-rotate(360deg); transform: rotate(.001deg); }\n}\n@keyframes fx-portal-pulse {\n  0%, 100% { transform: scale(.92); opacity: .24; }\n  50%      { transform: scale(1.08); opacity: .8; }\n}\n\n@keyframes pulse {\n  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 color-mix(in srgb, var(--primary) 50%, transparent); }\n  50% { opacity: .5; box-shadow: 0 0 0 6px transparent; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"PORTAL\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "tiltshift": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Tilt-Shift\" text effect applied to the word \"SHIFT\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-tiltshift\" data-text=\"SHIFT\">SHIFT</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 55 Tilt-Shift \u2014 a sharp focal strip travels through blurred duplicate text */\n.fx-tiltshift {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: color-mix(in srgb, var(--ink) 28%, transparent);\n  filter: blur(2px);\n}\n.fx-tiltshift::before,\n.fx-tiltshift::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-tiltshift::before {\n  color: var(--ink);\n  filter: blur(0);\n  clip-path: inset(32% 0 32% 0);\n  animation: fx-tiltshift 2.8s ease-in-out infinite alternate;\n}\n.fx-tiltshift::after {\n  color: var(--ink-3);\n  opacity: .3;\n  filter: blur(6px);\n}\n@keyframes fx-tiltshift {\n  0%   { clip-path: inset(0 0 64% 0); transform: translateY(-2px); }\n  100% { clip-path: inset(64% 0 0 0); transform: translateY(2px); }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"SHIFT\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "duotone": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Duotone\" text effect applied to the word \"DUO\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-duotone\" data-text=\"DUO\">DUO</div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 56 Duotone \u2014 two offset colour plates slide across a crisp centre layer */\n.fx-duotone {\n  position: relative;\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n}\n.fx-duotone::before,\n.fx-duotone::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  mix-blend-mode: screen;\n}\n.fx-duotone::before {\n  color: var(--ink-2);\n  animation: fx-duotone-a 2.6s ease-in-out infinite;\n}\n.fx-duotone::after {\n  color: var(--ink-3);\n  animation: fx-duotone-b 2.6s ease-in-out infinite;\n}\n@keyframes fx-duotone-a {\n  0%, 100% { transform: translate(-3px, 0); opacity: .42; }\n  50%      { transform: translate(-9px, -3px); opacity: .8; }\n}\n@keyframes fx-duotone-b {\n  0%, 100% { transform: translate(3px, 0); opacity: .42; }\n  50%      { transform: translate(9px, 3px); opacity: .8; }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"DUO\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone.", "glyphrain": "Recreate this animated text effect exactly, using pure CSS only \u2014 no JavaScript, no external libraries, no dependencies.\n\nIt is a \"Glyph-Rain\" text effect applied to the word \"GLYPHS\": the animation must match the CSS below precisely (same motion, timing curves, and colours).\n\nUse this exact HTML markup:\n\n<div class=\"fx-glyphrain\" role=\"img\" aria-label=\"GLYPHS\"><b aria-hidden=\"true\" style=\"--i:0\">G</b><b aria-hidden=\"true\" style=\"--i:1\">L</b><b aria-hidden=\"true\" style=\"--i:2\">Y</b><b aria-hidden=\"true\" style=\"--i:3\">P</b><b aria-hidden=\"true\" style=\"--i:4\">H</b><b aria-hidden=\"true\" style=\"--i:5\">S</b></div>\n\nApply this CSS:\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 57 Glyph-Rain \u2014 characters fall in staggered vertical trails and re-form */\n.fx-glyphrain {\n  font: 600 28px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-glyphrain b {\n  font-weight: inherit;\n  display: inline-block;\n  text-shadow:\n    0 -12px 0 color-mix(in srgb, var(--ink-3) 45%, transparent),\n    0 -24px 0 color-mix(in srgb, var(--ink-2) 28%, transparent);\n  animation: fx-glyphrain 2.4s cubic-bezier(.25, .8, .25, 1) infinite;\n  animation-delay: calc(var(--i) * .12s);\n}\n.fx-glyphrain b:nth-child(2n) { color: var(--ink-3); }\n@keyframes fx-glyphrain {\n  0%, 100% {\n    transform: translateY(0);\n    opacity: 1;\n  }\n  38% {\n    transform: translateY(22px);\n    opacity: .22;\n  }\n  64% {\n    transform: translateY(-10px);\n    opacity: .75;\n  }\n}\n\nRequirements:\n- Keep the markup and class names exactly as shown.\n- The effect paints with the `--ink`, `--ink-2` and `--ink-3` colour tokens \u2014 preserve them so the colours can be overridden.\n- The effect should work with any word, not just \"GLYPHS\" (per-letter effects index each letter with an inline `--i` custom property).\n- Respect `prefers-reduced-motion: reduce` by disabling the animation.\n- Do not add any JavaScript; the effect must be achieved with CSS alone."}, "snippets": {"aurora": "<!-- Markup: <div class=\"fx-aurora\">AURORA</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 01 Borealis \u2014 aurora gradient drifting through the letters */\n.fx-aurora {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  background: linear-gradient(115deg, var(--ink-2), var(--ink-3), var(--ink-2), var(--ink-3)) 0 0 / 300% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-aurora 7s linear infinite;\n}\n@keyframes fx-aurora {\n  0%   { background-position: 0% 50%;   filter: hue-rotate(0deg); }\n  100% { background-position: 300% 50%; filter: hue-rotate(360deg); }\n}", "glitch": "<!-- Markup: <div class=\"fx-glitch\" data-text=\"GLITCH\">GLITCH</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 02 Glitchcore \u2014 RGB channel-split glitch with sliced jumps */\n.fx-glitch {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n}\n.fx-glitch::before, .fx-glitch::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n}\n.fx-glitch::before {\n  color: var(--ink-2);\n  clip-path: inset(0 0 55% 0);\n  animation: fx-glitch-a 2.6s steps(1) infinite;\n}\n.fx-glitch::after {\n  color: var(--ink-3);\n  clip-path: inset(45% 0 0 0);\n  animation: fx-glitch-b 2.6s steps(1) infinite;\n}\n@keyframes fx-glitch-a {\n  0%, 12%, 20%, 55%, 100% { transform: translate(0); opacity: 0; }\n  13% { transform: translate(-5px, -2px); opacity: .9; }\n  16% { transform: translate(4px, 1px);   opacity: .9; }\n  48% { transform: translate(-3px, 2px);  opacity: .9; }\n  51% { transform: translate(2px, -1px);  opacity: .9; }\n}\n@keyframes fx-glitch-b {\n  0%, 14%, 22%, 60%, 100% { transform: translate(0); opacity: 0; }\n  15% { transform: translate(5px, 2px);   opacity: .9; }\n  18% { transform: translate(-4px, -1px); opacity: .9; }\n  52% { transform: translate(3px, -2px);  opacity: .9; }\n  56% { transform: translate(-2px, 1px);  opacity: .9; }\n}", "typewriter": "<!-- Markup: <div class=\"fx-typewriter\">TYPEWRITER</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 03 Teletype \u2014 typewriter with blinking caret (word must match the ch width) */\n.fx-typewriter {\n  font: 600 24px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: 0;\n  color: var(--ink);\n  white-space: nowrap;\n  overflow: hidden;\n  width: 10ch; /* = number of characters typed */\n  border-right: .55ch solid var(--ink-2);\n  animation: fx-type 3.6s steps(10) infinite,\n             fx-caret .65s steps(1) infinite;\n}\n@keyframes fx-type {\n  0%, 8%    { width: 0; }\n  55%, 100% { width: 10ch; }\n}\n@keyframes fx-caret {\n  50% { border-color: transparent; }\n}", "neon": "<!-- Markup: <div class=\"fx-neon\">NEON</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 04 Neon-Haus \u2014 buzzing neon sign with unstable tube flicker */\n.fx-neon {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .14em;\n  color: var(--ink);\n  animation: fx-neon 3.2s linear infinite;\n}\n@keyframes fx-neon {\n  0%, 18%, 21%, 24%, 100% {\n    opacity: 1;\n    text-shadow:\n      0 0 4px var(--ink),\n      0 0 11px var(--ink-2),\n      0 0 24px var(--ink-2),\n      0 0 48px var(--ink-2);\n  }\n  19%, 22%, 62% {\n    opacity: .35;\n    text-shadow: none;\n  }\n  63%, 64.5% {\n    opacity: .6;\n    text-shadow: 0 0 4px var(--ink-2);\n  }\n}", "liquid": "<!-- Markup: <div class=\"fx-liquid\">LIQUID</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 05 Aqua-Fill \u2014 the word fills with liquid that rises and drains */\n.fx-liquid {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: transparent;\n  -webkit-text-stroke: 1px color-mix(in srgb, var(--ink-3) 65%, transparent);\n  background: linear-gradient(0deg,\n      var(--ink-3) 0 46%,\n      color-mix(in srgb, var(--ink-3) 55%, transparent) 48%,\n      transparent 52% 100%\n    ) 0 0 / 100% 220%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-liquid 3.4s ease-in-out infinite;\n}\n@keyframes fx-liquid {\n  0%, 100% { background-position: 0 -110%; }\n  50%      { background-position: 0 10%; }\n}", "chrome": "<!-- Markup: <div class=\"fx-chrome\">CHROME</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 06 Chromia \u2014 brushed-metal letters with a passing sheen */\n.fx-chrome {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  background: linear-gradient(105deg,\n      color-mix(in srgb, var(--ink) 40%, transparent) 0 38%,\n      var(--ink) 47%,\n      #fff 50%,\n      var(--ink) 53%,\n      color-mix(in srgb, var(--ink) 40%, transparent) 62% 100%\n    ) 0 0 / 260% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-chrome 2.8s ease-in-out infinite;\n}\n@keyframes fx-chrome {\n  0%   { background-position: 130% 0; }\n  60%, 100% { background-position: -130% 0; }\n}", "focus": "<!-- Markup: <div class=\"fx-focus\" role=\"img\" aria-label=\"FOCUS\"><b aria-hidden=\"true\" style=\"--i:0\">F</b><b aria-hidden=\"true\" style=\"--i:1\">O</b><b aria-hidden=\"true\" style=\"--i:2\">C</b><b aria-hidden=\"true\" style=\"--i:3\">U</b><b aria-hidden=\"true\" style=\"--i:4\">S</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 07 Lens-Drift \u2014 letters slip in and out of focus, one after another */\n.fx-focus {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n}\n.fx-focus b {\n  font-weight: inherit;\n  display: inline-block;\n  animation: fx-focus 2.6s ease-in-out infinite;\n  animation-delay: calc(var(--i) * .18s);\n}\n@keyframes fx-focus {\n  0%, 100% { filter: blur(0);   opacity: 1; }\n  50%      { filter: blur(7px); opacity: .35; }\n}", "wave": "<!-- Markup: <div class=\"fx-wave\" role=\"img\" aria-label=\"WAVE\"><b aria-hidden=\"true\" style=\"--i:0\">W</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">V</b><b aria-hidden=\"true\" style=\"--i:3\">E</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 08 Tidal-Type \u2014 letters ride a rolling sine wave */\n.fx-wave {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink-3);\n}\n.fx-wave b {\n  font-weight: inherit;\n  display: inline-block;\n  animation: fx-wave 1.3s ease-in-out infinite;\n  animation-delay: calc(var(--i) * -.14s);\n}\n@keyframes fx-wave {\n  0%, 100% { transform: translateY(7px); }\n  50%      { transform: translateY(-7px); }\n}", "sliced": "<!-- Markup: <div class=\"fx-sliced\" data-text=\"SLICED\">SLICED</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 09 Bisect \u2014 the word is sliced horizontally; halves drift apart and re-align */\n.fx-sliced {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: transparent;\n}\n.fx-sliced::before, .fx-sliced::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: var(--ink);\n}\n.fx-sliced::before {\n  clip-path: inset(0 0 50% 0);\n  animation: fx-sliced-top 3s ease-in-out infinite;\n}\n.fx-sliced::after {\n  clip-path: inset(50% 0 0 0);\n  animation: fx-sliced-bot 3s ease-in-out infinite;\n}\n@keyframes fx-sliced-top {\n  0%, 22%, 78%, 100% { transform: translateX(0); }\n  38%, 62% { transform: translateX(8px); color: var(--ink-2); }\n}\n@keyframes fx-sliced-bot {\n  0%, 22%, 78%, 100% { transform: translateX(0); }\n  38%, 62% { transform: translateX(-8px); color: var(--ink-3); }\n}", "decoder": "<!-- Markup: <div class=\"fx-decoder\" role=\"img\" aria-label=\"DECODER\"></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 10 Cipher \u2014 scrambled characters decode into the word, then re-encrypt */\n.fx-decoder {\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink-3);\n}\n.fx-decoder::after {\n  content: \"\u2593K#R1&Q\";\n  animation: fx-decoder 4s steps(1) infinite;\n}\n@keyframes fx-decoder {\n  0%   { content: \"\u2593K#R1&Q\"; color: var(--ink-3); }\n  8%   { content: \"D%#OD3\u2592\"; }\n  16%  { content: \"DE\u2630O*ER\"; }\n  24%  { content: \"DEC0D\u00a3R\"; }\n  32%  { content: \"DECOD\u20acR\"; }\n  40%, 82% { content: \"DECODER\"; color: var(--ink); }\n  88%  { content: \"D3C0*\u20acR\"; color: var(--ink-3); }\n  94%  { content: \"\u2592#COD1Q\"; }\n}", "scanner": "<!-- Markup: <div class=\"fx-scanner\">SCANNER</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 11 Redactor \u2014 a scanning bar sweeps the word, lighting letters as it passes */\n.fx-scanner {\n  position: relative;\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  background: linear-gradient(90deg,\n      color-mix(in srgb, var(--ink) 25%, transparent) 0 40%,\n      var(--ink) 50%,\n      color-mix(in srgb, var(--ink) 25%, transparent) 60% 100%\n    ) 0 0 / 300% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-scan-glow 2.6s ease-in-out infinite alternate;\n}\n.fx-scanner::after {\n  content: \"\";\n  position: absolute;\n  top: -6px;\n  bottom: -6px;\n  left: 0;\n  width: 3px;\n  background: var(--ink-2);\n  box-shadow: 0 0 14px var(--ink-2);\n  animation: fx-scan-bar 2.6s ease-in-out infinite alternate;\n}\n@keyframes fx-scan-glow {\n  0%   { background-position: 100% 0; }\n  100% { background-position: 0% 0; }\n}\n@keyframes fx-scan-bar {\n  0%   { left: 0; transform: translateX(0); }\n  100% { left: 100%; transform: translateX(-100%); }\n}", "ember": "<!-- Markup: <div class=\"fx-ember\">EMBER</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 12 Emberglow \u2014 letters smoulder under rising fire light */\n.fx-ember {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: #ffd9a0;\n  animation: fx-ember 1.7s ease-in-out infinite alternate;\n}\n@keyframes fx-ember {\n  0% {\n    text-shadow:\n      0 -2px 6px  #ffab40,\n      0 -6px 14px #ff6d00,\n      0 -12px 28px #dd2c00,\n      0 -20px 44px rgba(213, 0, 0, .55);\n  }\n  100% {\n    text-shadow:\n      0 -3px 8px  #ffc46b,\n      0 -9px 20px #ff8f1f,\n      0 -18px 38px #ff3d00,\n      0 -30px 60px rgba(213, 0, 0, .8);\n  }\n}", "echo": "<!-- Markup: <div class=\"fx-echo\" data-text=\"ECHO\">ECHO</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 13 Echo-Verse \u2014 ghost copies of the word ripple outward */\n.fx-echo {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink);\n}\n.fx-echo::before, .fx-echo::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  animation: fx-echo 2s ease-out infinite;\n}\n.fx-echo::before { color: var(--ink-2); }\n.fx-echo::after  { color: var(--ink-3); animation-delay: 1s; }\n@keyframes fx-echo {\n  0%   { opacity: .8; transform: scale(1);   filter: blur(0); }\n  100% { opacity: 0;  transform: scale(1.9); filter: blur(3px); }\n}", "extrude": "<!-- Markup: <div class=\"fx-extrude\">DEPTH</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 14 Deep-Type \u2014 extruded 3D block letters rocking on their axis */\n.fx-extrude {\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  text-shadow:\n    1px 1px 0 color-mix(in srgb, var(--ink-2) 80%, #000),\n    2px 2px 0 color-mix(in srgb, var(--ink-2) 70%, #000),\n    3px 3px 0 color-mix(in srgb, var(--ink-2) 60%, #000),\n    4px 4px 0 color-mix(in srgb, var(--ink-2) 50%, #000),\n    5px 5px 0 color-mix(in srgb, var(--ink-2) 40%, #000),\n    6px 6px 0 color-mix(in srgb, var(--ink-2) 30%, #000),\n    7px 7px 12px rgba(0, 0, 0, .5);\n  animation: fx-extrude 3s ease-in-out infinite;\n}\n@keyframes fx-extrude {\n  0%, 100% { transform: rotate(-3.5deg) translateY(2px); }\n  50%      { transform: rotate(3.5deg)  translateY(-4px); }\n}", "contour": "<!-- Markup: <svg class=\"fx-contour\" viewBox=\"0 0 260 56\" role=\"img\" aria-label=\"OUTLINE\"><text x=\"50%\" y=\"50%\" dominant-baseline=\"central\" text-anchor=\"middle\">OUTLINE</text></svg> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 15 Wireframe \u2014 a dashed outline endlessly traces the letterforms */\n.fx-contour {\n  width: 240px;\n  height: 56px;\n  overflow: visible;\n}\n.fx-contour text {\n  font: 600 40px \"JetBrains Mono\", monospace;\n  letter-spacing: .02em;\n  fill: color-mix(in srgb, var(--ink) 10%, transparent);\n  stroke: var(--ink-3);\n  stroke-width: 1.2;\n  stroke-dasharray: 34 66;\n  animation: fx-contour 3.2s linear infinite;\n}\n@keyframes fx-contour {\n  to { stroke-dashoffset: -100; }\n}", "spectrum": "<!-- Markup: <div class=\"fx-spectrum\" role=\"img\" aria-label=\"SPECTRUM\"><b aria-hidden=\"true\" style=\"--i:0\">S</b><b aria-hidden=\"true\" style=\"--i:1\">P</b><b aria-hidden=\"true\" style=\"--i:2\">E</b><b aria-hidden=\"true\" style=\"--i:3\">C</b><b aria-hidden=\"true\" style=\"--i:4\">T</b><b aria-hidden=\"true\" style=\"--i:5\">R</b><b aria-hidden=\"true\" style=\"--i:6\">U</b><b aria-hidden=\"true\" style=\"--i:7\">M</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 16 Prisma \u2014 each letter refracts a different band of the spectrum */\n.fx-spectrum {\n  font: 600 26px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n}\n.fx-spectrum b {\n  font-weight: inherit;\n  display: inline-block;\n  color: #ff2d78;\n  filter: hue-rotate(calc(var(--i) * 42deg));\n  animation: fx-spectrum 2.4s linear infinite;\n  animation-delay: calc(var(--i) * -.1s);\n}\n@keyframes fx-spectrum {\n  to { filter: hue-rotate(calc(var(--i) * 42deg + 360deg)); }\n}", "jitter": "<!-- Markup: <div class=\"fx-jitter\">JITTER</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 17 Jitterbug \u2014 nervous analogue jitter: skews, jumps and dropped frames */\n.fx-jitter {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n  animation: fx-jitter 1.9s steps(1) infinite;\n}\n@keyframes fx-jitter {\n  0%, 7%, 15%, 55%, 100% { transform: none; opacity: 1; }\n  8%  { transform: skewX(-14deg) translateX(-4px); }\n  10% { transform: skewX(10deg) translateX(3px); }\n  12% { transform: translateY(-3px); opacity: .6; }\n  46% { transform: skewX(8deg); }\n  48% { transform: skewX(-6deg) translateY(2px); opacity: .75; }\n  50% { transform: translateX(2px); }\n}", "anaglyph": "<!-- Markup: <div class=\"fx-anaglyph\">DEPTH-3D</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 18 Anaglyph-3D \u2014 red/cyan stereo channels drift apart and snap back */\n.fx-anaglyph {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  animation: fx-anaglyph 2.4s ease-in-out infinite;\n}\n@keyframes fx-anaglyph {\n  0%, 100% { text-shadow: 0 0 0 #ff3355, 0 0 0 #33ddff; }\n  50% {\n    text-shadow:\n      -6px 0 1px #ff3355,\n       6px 0 1px #33ddff;\n  }\n}", "flap": "<!-- Markup: <div class=\"fx-flap\" role=\"img\" aria-label=\"DEPART\"><b aria-hidden=\"true\" style=\"--i:0\">D</b><b aria-hidden=\"true\" style=\"--i:1\">E</b><b aria-hidden=\"true\" style=\"--i:2\">P</b><b aria-hidden=\"true\" style=\"--i:3\">A</b><b aria-hidden=\"true\" style=\"--i:4\">R</b><b aria-hidden=\"true\" style=\"--i:5\">T</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 19 Split-Flap \u2014 airport departure board; letters flip on their plates */\n.fx-flap {\n  font: 600 24px/1 \"JetBrains Mono\", monospace;\n  letter-spacing: 0;\n  display: inline-flex;\n  gap: 3px;\n  perspective: 400px;\n  color: var(--ink);\n}\n.fx-flap b {\n  font-weight: inherit;\n  display: grid;\n  place-items: center;\n  width: 1.5em;\n  height: 1.9em;\n  background: linear-gradient(\n      color-mix(in srgb, var(--ink) 16%, transparent) 0 49%,\n      color-mix(in srgb, var(--ink) 8%, transparent) 51% 100%);\n  border-radius: 4px;\n  transform-origin: center;\n  animation: fx-flap 3.2s ease-in-out infinite;\n  animation-delay: calc(var(--i) * .12s);\n}\n@keyframes fx-flap {\n  0%, 55%, 100% { transform: rotateX(0); }\n  62% { transform: rotateX(-88deg); }\n  69% { transform: rotateX(0); }\n  74% { transform: rotateX(-25deg); }\n  79% { transform: rotateX(0); }\n}", "crt": "<!-- Markup: <div class=\"fx-crt\">CRT_MODE</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 20 Phosphor \u2014 CRT terminal: green glow, rolling scanlines, tube flicker */\n.fx-crt {\n  position: relative;\n  font: 600 26px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  padding: .3em .5em;\n  color: #4dff88;\n  text-shadow: 0 0 7px rgba(77, 255, 136, .8);\n  background: rgba(20, 60, 35, .18);\n  border-radius: 4px;\n  animation: fx-crt-flicker 3.4s steps(1) infinite;\n}\n.fx-crt::after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  border-radius: inherit;\n  pointer-events: none;\n  background: repeating-linear-gradient(0deg,\n      transparent 0 2px,\n      rgba(0, 0, 0, .38) 2px 4px);\n  animation: fx-crt-roll 6s linear infinite;\n}\n@keyframes fx-crt-flicker {\n  0%, 6%, 100% { opacity: 1; }\n  3%   { opacity: .82; }\n  4.5% { opacity: .95; transform: translateY(1px); }\n  70%  { opacity: .9; }\n  71%  { opacity: 1; transform: none; }\n}\n@keyframes fx-crt-roll {\n  to { background-position: 0 48px; }\n}", "pop": "<!-- Markup: <div class=\"fx-pop\" role=\"img\" aria-label=\"POP!\"><b aria-hidden=\"true\" style=\"--i:0\">P</b><b aria-hidden=\"true\" style=\"--i:1\">O</b><b aria-hidden=\"true\" style=\"--i:2\">P</b><b aria-hidden=\"true\" style=\"--i:3\">!</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 21 Pop-Riot \u2014 letters leap with a hard comic-book drop shadow */\n.fx-pop {\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .04em;\n  color: var(--ink);\n}\n.fx-pop b {\n  font-weight: inherit;\n  display: inline-block;\n  animation: fx-pop 1.5s cubic-bezier(.3, 1.6, .4, 1) infinite;\n  animation-delay: calc(var(--i) * .09s);\n}\n@keyframes fx-pop {\n  0%, 55%, 100% { transform: translateY(0); text-shadow: 0 0 0 var(--ink-2); }\n  25% { transform: translateY(-12px) rotate(-4deg); text-shadow: 0 12px 0 var(--ink-2); }\n}", "spotlight": "<!-- Markup: <div class=\"fx-spotlight\">SPOTLIGHT</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 22 Limelight \u2014 a spotlight sweeps across otherwise unlit letters */\n.fx-spotlight {\n  font: 600 26px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  background: radial-gradient(3.5ch 100% at 50% 50%,\n      var(--ink) 20%,\n      color-mix(in srgb, var(--ink) 14%, transparent) 75%\n    ) 0 0 / 300% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  color: transparent;\n  animation: fx-spotlight 3.4s ease-in-out infinite alternate;\n}\n@keyframes fx-spotlight {\n  0%   { background-position: 100% 0; }\n  100% { background-position: 0% 0; }\n}", "elastic": "<!-- Markup: <div class=\"fx-elastic\">BOING</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 23 Rubber-Band \u2014 the word squashes and stretches like an elastic band */\n.fx-elastic {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink-2);\n  animation: fx-elastic 1.6s ease-in-out infinite;\n}\n@keyframes fx-elastic {\n  0%, 100% { transform: scale(1, 1); }\n  28% { transform: scale(1.28, .72); }\n  44% { transform: scale(.78, 1.24); }\n  60% { transform: scale(1.12, .9); }\n  74% { transform: scale(.96, 1.05); }\n  86% { transform: scale(1.02, .98); }\n}", "mirror": "<!-- Markup: <div class=\"fx-mirror\">MIRROR</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 24 Still-Water \u2014 the word floats over its own rippling reflection\n   (uses -webkit-box-reflect: Chromium & Safari; degrades gracefully) */\n.fx-mirror {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  -webkit-box-reflect: below 2px linear-gradient(transparent 45%, rgba(255, 255, 255, .28));\n  animation: fx-mirror 3s ease-in-out infinite;\n}\n@keyframes fx-mirror {\n  0%, 100% { transform: translateY(0); }\n  50%      { transform: translateY(-6px); }\n}", "ransom": "<!-- Markup: <div class=\"fx-ransom\" role=\"img\" aria-label=\"RANSOM\"><b aria-hidden=\"true\" style=\"--i:0\">R</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">N</b><b aria-hidden=\"true\" style=\"--i:3\">S</b><b aria-hidden=\"true\" style=\"--i:4\">O</b><b aria-hidden=\"true\" style=\"--i:5\">M</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 25 Ransom-Note \u2014 mismatched cut-out letters shifting on the page */\n.fx-ransom {\n  font: 600 24px/1 \"JetBrains Mono\", monospace;\n  letter-spacing: 0;\n  display: inline-flex;\n  gap: 4px;\n  color: var(--ink);\n}\n.fx-ransom b {\n  font-weight: inherit;\n  display: grid;\n  place-items: center;\n  padding: .28em .3em;\n  border-radius: 3px;\n  background: color-mix(in srgb, var(--ink) 14%, transparent);\n  animation: fx-ransom 2.8s ease-in-out infinite alternate;\n  animation-delay: calc(var(--i) * -.5s);\n}\n.fx-ransom b:nth-child(odd) {\n  background: var(--ink-2);\n  color: #14020f;\n}\n.fx-ransom b:nth-child(3n) {\n  background: transparent;\n  outline: 1.5px dashed color-mix(in srgb, var(--ink) 55%, transparent);\n  color: var(--ink-3);\n}\n@keyframes fx-ransom {\n  0%   { transform: rotate(-6deg) translateY(1.5px); }\n  100% { transform: rotate(6deg)  translateY(-1.5px); }\n}", "melt": "<!-- Markup: <div class=\"fx-melt\" role=\"img\" aria-label=\"MELT\"><b aria-hidden=\"true\" style=\"--i:0\">M</b><b aria-hidden=\"true\" style=\"--i:1\">E</b><b aria-hidden=\"true\" style=\"--i:2\">L</b><b aria-hidden=\"true\" style=\"--i:3\">T</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 26 Meltdown \u2014 letters sag, stretch and drip before snapping back */\n.fx-melt {\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink-2);\n}\n.fx-melt b {\n  font-weight: inherit;\n  display: inline-block;\n  transform-origin: top center;\n  animation: fx-melt 2.8s ease-in infinite;\n  animation-delay: calc(var(--i) * .22s);\n}\n@keyframes fx-melt {\n  0%, 45%  { transform: scaleY(1) translateY(0); filter: blur(0); }\n  70%      { transform: scaleY(1.55) translateY(7px) scaleX(.92); filter: blur(1.5px); }\n  85%, 100% { transform: scaleY(1) translateY(0); filter: blur(0); }\n}", "heartbeat": "<!-- Markup: <div class=\"fx-heartbeat\">PULSE</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 27 Cardio \u2014 a lub-dub heartbeat with a systolic glow */\n.fx-heartbeat {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink-2);\n  animation: fx-heartbeat 1.4s ease-in-out infinite;\n}\n@keyframes fx-heartbeat {\n  0%, 100% { transform: scale(1); text-shadow: 0 0 0 transparent; }\n  14% { transform: scale(1.14); text-shadow: 0 0 18px color-mix(in srgb, var(--ink-2) 65%, transparent); }\n  28% { transform: scale(1); }\n  42% { transform: scale(1.18); text-shadow: 0 0 26px color-mix(in srgb, var(--ink-2) 80%, transparent); }\n  70% { transform: scale(1); text-shadow: 0 0 0 transparent; }\n}", "marker": "<!-- Markup: <div class=\"fx-marker\">MARKED</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 28 Hi-Liter \u2014 a marker pen paints across the word, then lifts off */\n.fx-marker {\n  font: 600 28px/1.3 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  padding: .1em .2em;\n  background: linear-gradient(100deg,\n      color-mix(in srgb, var(--ink-2) 85%, transparent),\n      color-mix(in srgb, var(--ink-2) 60%, transparent)\n    ) no-repeat 0 62%;\n  background-size: 0% 46%;\n  animation: fx-marker 3s ease-in-out infinite;\n}\n@keyframes fx-marker {\n  0%        { background-size: 0% 46%;   background-position: 0 62%; }\n  38%, 62%  { background-size: 100% 46%; background-position: 0 62%; }\n  100%      { background-size: 0% 46%;   background-position: 100% 62%; }\n}", "sundial": "<!-- Markup: <div class=\"fx-sundial\">SHADOW</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 29 Sundial \u2014 a long cast shadow circles the letters like a passing sun */\n.fx-sundial {\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  animation: fx-sundial 5s linear infinite;\n}\n@keyframes fx-sundial {\n  0%, 100% {\n    text-shadow:\n      4px 0 var(--ink-2),\n      8px 0 color-mix(in srgb, var(--ink-2) 55%, transparent),\n      12px 0 color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  12.5% {\n    text-shadow:\n      2.8px 2.8px var(--ink-2),\n      5.6px 5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      8.4px 8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  25% {\n    text-shadow:\n      0 4px var(--ink-2),\n      0 8px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      0 12px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  37.5% {\n    text-shadow:\n      -2.8px 2.8px var(--ink-2),\n      -5.6px 5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      -8.4px 8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  50% {\n    text-shadow:\n      -4px 0 var(--ink-2),\n      -8px 0 color-mix(in srgb, var(--ink-2) 55%, transparent),\n      -12px 0 color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  62.5% {\n    text-shadow:\n      -2.8px -2.8px var(--ink-2),\n      -5.6px -5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      -8.4px -8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  75% {\n    text-shadow:\n      0 -4px var(--ink-2),\n      0 -8px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      0 -12px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n  87.5% {\n    text-shadow:\n      2.8px -2.8px var(--ink-2),\n      5.6px -5.6px color-mix(in srgb, var(--ink-2) 55%, transparent),\n      8.4px -8.4px color-mix(in srgb, var(--ink-2) 25%, transparent);\n  }\n}", "negative": "<!-- Markup: <div class=\"fx-negative\">INVERTED</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 30 Negative \u2014 an inversion bar sweeps the word, flipping letters to their\n   photographic negative via mix-blend-mode: difference */\n.fx-negative {\n  position: relative;\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  padding: .18em .22em;\n}\n.fx-negative::after {\n  content: \"\";\n  position: absolute;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 34%;\n  background: var(--ink);\n  mix-blend-mode: difference;\n  animation: fx-negative 2.4s ease-in-out infinite alternate;\n}\n@keyframes fx-negative {\n  0%   { left: 0;    transform: translateX(0); }\n  100% { left: 100%; transform: translateX(-100%); }\n}", "hologram": "<!-- Markup: <div class=\"fx-hologram\" data-text=\"HOLOGRAM\">HOLOGRAM</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 31 Holograph \u2014 translucent scanline projection with drifting chroma edges */\n.fx-hologram {\n  position: relative;\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: color-mix(in srgb, var(--ink-3) 72%, var(--ink));\n  text-shadow:\n    0 0 6px color-mix(in srgb, var(--ink-3) 60%, transparent),\n    0 0 22px color-mix(in srgb, var(--ink-3) 45%, transparent);\n  isolation: isolate;\n  animation: fx-hologram-float 3.8s ease-in-out infinite;\n}\n.fx-hologram::before,\n.fx-hologram::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-hologram::before {\n  color: var(--ink-2);\n  clip-path: inset(0 0 52% 0);\n  opacity: .58;\n  animation: fx-hologram-split 2.8s steps(1) infinite;\n}\n.fx-hologram::after {\n  color: transparent;\n  background: repeating-linear-gradient(\n      0deg,\n      color-mix(in srgb, var(--ink) 70%, transparent) 0 1px,\n      transparent 1px 4px\n    );\n  -webkit-background-clip: text;\n  background-clip: text;\n  opacity: .48;\n  animation: fx-hologram-lines 1.2s linear infinite;\n}\n@keyframes fx-hologram-float {\n  0%, 100% { transform: translateY(2px) skewX(0deg); filter: brightness(1); }\n  50%      { transform: translateY(-4px) skewX(-3deg); filter: brightness(1.35); }\n}\n@keyframes fx-hologram-split {\n  0%, 14%, 40%, 72%, 100% { transform: translateX(0); opacity: .22; }\n  15% { transform: translateX(-5px); opacity: .8; }\n  17% { transform: translateX(4px);  opacity: .6; }\n  74% { transform: translateX(3px);  opacity: .7; }\n}\n@keyframes fx-hologram-lines {\n  to { background-position: 0 12px; }\n}", "foil": "<!-- Markup: <div class=\"fx-foil\">LUXE</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 32 Gold-Foil \u2014 embossed metallic letters with a diagonal specular sweep */\n.fx-foil {\n  font: 600 36px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: transparent;\n  background:\n    linear-gradient(108deg,\n      #7a4a08 0%,\n      #f2c15d 18%,\n      #fff4b8 24%,\n      #b87014 36%,\n      #f6d66d 51%,\n      #8e560b 68%,\n      #fff0a2 76%,\n      #c98218 100%\n    ) 0 0 / 230% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  text-shadow:\n    0 1px 0 rgba(255, 244, 184, .45),\n    0 9px 20px rgba(0, 0, 0, .36);\n  animation: fx-foil 3.1s ease-in-out infinite;\n}\n@keyframes fx-foil {\n  0%, 100% { background-position: 115% 0; filter: saturate(1.05); }\n  45%      { background-position: -35% 0; filter: saturate(1.35) brightness(1.12); }\n}", "pixel": "<!-- Markup: <div class=\"fx-pixel\" data-text=\"PIXELS\">PIXELS</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 33 Pixel-Sort \u2014 blocky offsets smear the word into stepped digital columns */\n.fx-pixel {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  text-shadow:\n    1px 0 0 var(--ink-2),\n   -1px 0 0 var(--ink-3);\n}\n.fx-pixel::before,\n.fx-pixel::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  opacity: .85;\n}\n.fx-pixel::before {\n  color: var(--ink-2);\n  clip-path: polygon(0 0, 100% 0, 100% 28%, 0 28%, 0 44%, 100% 44%, 100% 58%, 0 58%);\n  animation: fx-pixel-a 2.2s steps(1) infinite;\n}\n.fx-pixel::after {\n  color: var(--ink-3);\n  clip-path: polygon(0 30%, 100% 30%, 100% 42%, 0 42%, 0 66%, 100% 66%, 100% 82%, 0 82%);\n  animation: fx-pixel-b 2.2s steps(1) infinite;\n}\n@keyframes fx-pixel-a {\n  0%, 20%, 54%, 100% { transform: translate(0); }\n  22% { transform: translate(7px, -2px); }\n  24% { transform: translate(-4px, 1px); }\n  56% { transform: translate(5px, 2px); }\n  58% { transform: translate(-7px, 0); }\n}\n@keyframes fx-pixel-b {\n  0%, 18%, 58%, 100% { transform: translate(0); }\n  19% { transform: translate(-6px, 2px); }\n  21% { transform: translate(4px, -1px); }\n  60% { transform: translate(-5px, -2px); }\n  63% { transform: translate(6px, 1px); }\n}", "starlight": "<!-- Markup: <div class=\"fx-starlight\">STARDUST</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 34 Starlight \u2014 a constellation shimmer twinkles across frosted letter faces */\n.fx-starlight {\n  font: 600 28px/1.2 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background:\n    radial-gradient(circle at 14% 35%, #fff 0 2px, transparent 2.4px),\n    radial-gradient(circle at 42% 68%, #fff 0 1.6px, transparent 2px),\n    radial-gradient(circle at 75% 24%, #fff 0 2.2px, transparent 2.6px),\n    linear-gradient(100deg, var(--ink), var(--ink-3), var(--ink));\n  background-size: 120px 60px, 150px 72px, 180px 68px, 240% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  filter: drop-shadow(0 0 10px color-mix(in srgb, var(--ink-3) 45%, transparent));\n  animation: fx-starlight 3.6s linear infinite;\n}\n@keyframes fx-starlight {\n  0%   { background-position: 0 0, 0 0, 0 0, 120% 0; }\n  100% { background-position: 120px 0, -150px 0, 180px 0, -120% 0; }\n}", "blueprint": "<!-- Markup: <div class=\"fx-blueprint\">DRAFT</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 35 Blueprint \u2014 technical outline letters over a moving drafting grid */\n.fx-blueprint {\n  font: 600 28px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: transparent;\n  padding: .28em .45em;\n  -webkit-text-stroke: 1px color-mix(in srgb, var(--ink-3) 80%, var(--ink));\n  background:\n    linear-gradient(90deg, color-mix(in srgb, var(--ink-3) 22%, transparent) 1px, transparent 1px),\n    linear-gradient(0deg, color-mix(in srgb, var(--ink-3) 18%, transparent) 1px, transparent 1px),\n    linear-gradient(90deg, color-mix(in srgb, var(--ink-3) 42%, transparent), transparent 35% 65%, color-mix(in srgb, var(--ink-3) 42%, transparent));\n  background-size: 12px 12px, 12px 12px, 240% 100%;\n  background-position: 0 0, 0 0, 100% 0;\n  border: 1px solid color-mix(in srgb, var(--ink-3) 42%, transparent);\n  box-shadow: inset 0 0 18px color-mix(in srgb, var(--ink-3) 18%, transparent);\n  animation: fx-blueprint 4s linear infinite;\n}\n@keyframes fx-blueprint {\n  to { background-position: 24px 0, 0 24px, -100% 0; }\n}", "vapor": "<!-- Markup: <div class=\"fx-vapor\" data-text=\"VAPOR\">VAPOR</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 36 Vapor-Trail \u2014 soft neon copies trail behind a drifting word */\n.fx-vapor {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .16em;\n  color: var(--ink);\n  text-shadow: 0 0 12px color-mix(in srgb, var(--ink) 45%, transparent);\n  animation: fx-vapor-main 2.8s ease-in-out infinite;\n}\n.fx-vapor::before,\n.fx-vapor::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  filter: blur(2px);\n  opacity: .62;\n}\n.fx-vapor::before {\n  color: var(--ink-2);\n  animation: fx-vapor-a 2.8s ease-in-out infinite;\n}\n.fx-vapor::after {\n  color: var(--ink-3);\n  animation: fx-vapor-b 2.8s ease-in-out infinite;\n}\n@keyframes fx-vapor-main {\n  0%, 100% { transform: translateX(-3px); }\n  50%      { transform: translateX(3px); }\n}\n@keyframes fx-vapor-a {\n  0%, 100% { transform: translate(-10px, 6px) scaleX(1.08); opacity: .22; }\n  50%      { transform: translate(-18px, -4px) scaleX(1.22); opacity: .58; }\n}\n@keyframes fx-vapor-b {\n  0%, 100% { transform: translate(10px, -5px) scaleX(1.08); opacity: .2; }\n  50%      { transform: translate(18px, 5px) scaleX(1.2); opacity: .56; }\n}", "kinetic": "<!-- Markup: <div class=\"fx-kinetic\" role=\"img\" aria-label=\"KINETIC\"><b aria-hidden=\"true\" style=\"--i:0\">K</b><b aria-hidden=\"true\" style=\"--i:1\">I</b><b aria-hidden=\"true\" style=\"--i:2\">N</b><b aria-hidden=\"true\" style=\"--i:3\">E</b><b aria-hidden=\"true\" style=\"--i:4\">T</b><b aria-hidden=\"true\" style=\"--i:5\">I</b><b aria-hidden=\"true\" style=\"--i:6\">C</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 37 Kinetic-Type \u2014 characters slide on staggered rails and lock into place */\n.fx-kinetic {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-kinetic b {\n  font-weight: inherit;\n  display: inline-block;\n  transform-origin: center;\n  animation: fx-kinetic 2.6s cubic-bezier(.2, .8, .2, 1) infinite;\n  animation-delay: calc(var(--i) * .08s);\n}\n.fx-kinetic b:nth-child(2n) { color: var(--ink-3); }\n.fx-kinetic b:nth-child(3n) { color: var(--ink-2); }\n@keyframes fx-kinetic {\n  0%, 18%   { transform: translateY(0) rotate(0deg); filter: blur(0); }\n  34%       { transform: translateY(-18px) rotate(-9deg); filter: blur(.4px); }\n  52%       { transform: translateY(14px) rotate(7deg); }\n  68%, 100% { transform: translateY(0) rotate(0deg); filter: blur(0); }\n}", "blackout": "<!-- Markup: <div class=\"fx-blackout\" data-text=\"CLASSIFIED\">CLASSIFIED</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 38 Blackout \u2014 redaction strips repeatedly hide and reveal classified text */\n.fx-blackout {\n  position: relative;\n  font: 600 24px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  padding: .28em .12em;\n}\n.fx-blackout::before,\n.fx-blackout::after {\n  content: \"\";\n  position: absolute;\n  left: -.18em;\n  right: -.18em;\n  height: 44%;\n  background: var(--ink);\n  box-shadow: 0 0 0 1px color-mix(in srgb, var(--ink-2) 45%, transparent);\n  animation: fx-blackout 3s steps(1) infinite;\n}\n.fx-blackout::before {\n  top: 0;\n  transform-origin: left;\n}\n.fx-blackout::after {\n  bottom: 0;\n  transform-origin: right;\n  animation-delay: .28s;\n}\n@keyframes fx-blackout {\n  0%, 20%, 72%, 100% { transform: scaleX(0); }\n  26%, 58%           { transform: scaleX(1); }\n  64%                { transform: scaleX(.38); }\n}\n\n@keyframes reveal {\n  from { opacity: 0; transform: translateY(20px); clip-path: inset(0 0 100% 0); }\n  to   { opacity: 1; transform: translateY(0);    clip-path: inset(0 0 0 0); }\n}", "magnetic": "<!-- Markup: <div class=\"fx-magnetic\" role=\"img\" aria-label=\"MAGNET\"><b aria-hidden=\"true\" style=\"--i:0\">M</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">G</b><b aria-hidden=\"true\" style=\"--i:3\">N</b><b aria-hidden=\"true\" style=\"--i:4\">E</b><b aria-hidden=\"true\" style=\"--i:5\">T</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 39 Magnetic \u2014 letters attract into a tight field, then repel outward */\n.fx-magnetic {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .06em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-magnetic b {\n  font-weight: inherit;\n  display: inline-block;\n  text-shadow: 0 0 0 var(--ink-2);\n  animation: fx-magnetic 2.7s ease-in-out infinite;\n  animation-delay: calc(var(--i) * .07s);\n}\n.fx-magnetic b:nth-child(odd) { color: var(--ink-3); }\n@keyframes fx-magnetic {\n  0%, 100% {\n    transform: translateX(0) scale(1);\n    text-shadow: 0 0 0 transparent;\n  }\n  42% {\n    transform: translateX(calc((3 - var(--i)) * 2px)) scale(1.12);\n    text-shadow: 0 0 16px color-mix(in srgb, var(--ink-2) 70%, transparent);\n  }\n  62% {\n    transform: translateX(calc((var(--i) - 3) * 3px)) scale(.96);\n  }\n}", "mesh": "<!-- Markup: <div class=\"fx-mesh\">MESH</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 40 Luma-Mesh \u2014 animated mesh-gradient ink with a soft luminous edge */\n.fx-mesh {\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: transparent;\n  background:\n    radial-gradient(circle at 18% 28%, var(--ink-2) 0 12%, transparent 28%),\n    radial-gradient(circle at 78% 24%, var(--ink-3) 0 14%, transparent 30%),\n    radial-gradient(circle at 44% 82%, #ffffff 0 10%, transparent 24%),\n    linear-gradient(110deg, var(--ink-2), var(--ink-3));\n  background-size: 180% 180%, 170% 170%, 160% 160%, 220% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  filter: drop-shadow(0 0 18px color-mix(in srgb, var(--ink-3) 28%, transparent));\n  animation: fx-mesh 4.8s ease-in-out infinite alternate;\n}\n@keyframes fx-mesh {\n  0%   { background-position: 0% 20%, 100% 10%, 35% 100%, 100% 0; }\n  100% { background-position: 100% 80%, 0% 60%, 70% 0%, -100% 0; }\n}", "iridescent": "<!-- Markup: <div class=\"fx-iridescent\">IRIS</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 41 Iridescent \u2014 pearlescent text whose colour shifts with a moving angle */\n.fx-iridescent {\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background: conic-gradient(\n      from 20deg,\n      #f7fbff,\n      var(--ink-3),\n      #bba7ff,\n      var(--ink-2),\n      #fff7d6,\n      #f7fbff\n    ) 50% 50% / 220% 220%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  text-shadow: 0 10px 28px rgba(0, 0, 0, .28);\n  animation: fx-iridescent 4.5s linear infinite;\n}\n@keyframes fx-iridescent {\n  to { filter: hue-rotate(360deg); background-position: 120% 50%; }\n}", "glass": "<!-- Markup: <div class=\"fx-glass\" data-text=\"GLASS\">GLASS</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 42 Glass-Type \u2014 translucent frosted letters with an offset refraction layer */\n.fx-glass {\n  position: relative;\n  font: 600 34px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: color-mix(in srgb, var(--ink) 58%, transparent);\n  -webkit-text-stroke: 1px color-mix(in srgb, var(--ink) 52%, transparent);\n  text-shadow:\n    0 1px 0 rgba(255, 255, 255, .4),\n    0 12px 24px rgba(0, 0, 0, .3);\n  animation: fx-glass 3.4s ease-in-out infinite;\n}\n.fx-glass::before {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: var(--ink-3);\n  opacity: .34;\n  filter: blur(3px);\n  transform: translate(4px, 5px);\n}\n.fx-glass::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: transparent;\n  background: linear-gradient(115deg, transparent 22%, #fff 44%, transparent 62%) 110% 0 / 260% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  opacity: .55;\n  animation: fx-glass-sheen 2.7s ease-in-out infinite;\n}\n@keyframes fx-glass {\n  50% { transform: translateY(-3px); }\n}\n@keyframes fx-glass-sheen {\n  to { background-position: -110% 0; }\n}", "datastream": "<!-- Markup: <div class=\"fx-datastream\">STREAM</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 43 Datastream \u2014 matrix-like data bands flow through clipped letterforms */\n.fx-datastream {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background:\n    repeating-linear-gradient(0deg,\n      var(--ink-3) 0 2px,\n      color-mix(in srgb, var(--ink-3) 35%, transparent) 2px 4px,\n      transparent 4px 8px\n    ),\n    linear-gradient(90deg, var(--ink-2), var(--ink-3), var(--ink));\n  background-size: 100% 32px, 240% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-datastream 1.6s linear infinite;\n}\n.fx-datastream::after {\n  content: \"0101 1100 0011\";\n  position: absolute;\n  left: 50%;\n  top: 100%;\n  width: 170%;\n  color: color-mix(in srgb, var(--ink-3) 42%, transparent);\n  font-size: .42em;\n  letter-spacing: .16em;\n  transform: translateX(-50%);\n  animation: fx-datastream-code 1.6s linear infinite;\n}\n@keyframes fx-datastream {\n  to { background-position: 0 32px, -120% 0; }\n}\n@keyframes fx-datastream-code {\n  0% { opacity: .2; transform: translate(-50%, -2px); }\n  50% { opacity: .7; }\n  100% { opacity: .2; transform: translate(-50%, 10px); }\n}", "orbit": "<!-- Markup: <div class=\"fx-orbit\" role=\"img\" aria-label=\"ORBIT\"><b aria-hidden=\"true\" style=\"--i:0\">O</b><b aria-hidden=\"true\" style=\"--i:1\">R</b><b aria-hidden=\"true\" style=\"--i:2\">B</b><b aria-hidden=\"true\" style=\"--i:3\">I</b><b aria-hidden=\"true\" style=\"--i:4\">T</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 44 Orbitals \u2014 letters circle tiny invisible tracks before settling back */\n.fx-orbit {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-orbit b {\n  font-weight: inherit;\n  display: inline-block;\n  transform-origin: 50% 120%;\n  animation: fx-orbit 2.8s cubic-bezier(.45, 0, .2, 1) infinite;\n  animation-delay: calc(var(--i) * -.18s);\n}\n.fx-orbit b:nth-child(odd) { color: var(--ink-3); }\n.fx-orbit b:nth-child(3n) { color: var(--ink-2); }\n@keyframes fx-orbit {\n  0%, 100% { transform: translate(0, 0) rotate(0deg); }\n  35%      { transform: translate(5px, -13px) rotate(16deg); }\n  70%      { transform: translate(-5px, 7px) rotate(-10deg); }\n}", "prismcut": "<!-- Markup: <div class=\"fx-prismcut\" data-text=\"PRISM\">PRISM</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 45 Prism-Cut \u2014 diagonal glass shards refract the word into colour offsets */\n.fx-prismcut {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink);\n}\n.fx-prismcut::before,\n.fx-prismcut::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-prismcut::before {\n  color: var(--ink-2);\n  clip-path: polygon(0 0, 66% 0, 46% 100%, 0 100%);\n  animation: fx-prismcut-a 3s ease-in-out infinite;\n}\n.fx-prismcut::after {\n  color: var(--ink-3);\n  clip-path: polygon(42% 0, 100% 0, 100% 100%, 24% 100%);\n  animation: fx-prismcut-b 3s ease-in-out infinite;\n}\n@keyframes fx-prismcut-a {\n  0%, 100% { transform: translate(0); opacity: .35; }\n  50%      { transform: translate(-7px, -2px); opacity: .85; }\n}\n@keyframes fx-prismcut-b {\n  0%, 100% { transform: translate(0); opacity: .35; }\n  50%      { transform: translate(7px, 2px); opacity: .85; }\n}", "softblur": "<!-- Markup: <div class=\"fx-softblur\">FOCUS</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 46 Soft-Blur \u2014 a modern bloom effect that breathes between crisp and hazy */\n.fx-softblur {\n  font: 600 36px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  text-shadow:\n    0 0 0 var(--ink),\n    0 0 18px color-mix(in srgb, var(--ink-2) 45%, transparent),\n    0 0 34px color-mix(in srgb, var(--ink-3) 35%, transparent);\n  animation: fx-softblur 3.2s ease-in-out infinite;\n}\n@keyframes fx-softblur {\n  0%, 100% { filter: blur(0); opacity: 1; transform: scale(1); }\n  50%      { filter: blur(2.4px); opacity: .72; transform: scale(1.045); }\n}", "laser": "<!-- Markup: <div class=\"fx-laser\" data-text=\"LASER\">LASER</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 47 Laser-Cut \u2014 a hot scanning blade carves a bright split through the word */\n.fx-laser {\n  position: relative;\n  font: 600 34px/1.08 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: var(--ink);\n}\n.fx-laser::before {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: var(--ink-2);\n  clip-path: inset(44% 0 42% 0);\n  text-shadow: 0 0 10px var(--ink-2);\n  animation: fx-laser-slice 2.2s ease-in-out infinite;\n}\n.fx-laser::after {\n  content: \"\";\n  position: absolute;\n  left: -8%;\n  top: 49%;\n  width: 116%;\n  height: 2px;\n  background: linear-gradient(90deg, transparent, #fff, var(--ink-2), transparent);\n  box-shadow: 0 0 13px var(--ink-2);\n  animation: fx-laser 2.2s ease-in-out infinite;\n}\n@keyframes fx-laser {\n  0%, 100% { transform: translateY(-14px) scaleX(.35); opacity: 0; }\n  44%, 58% { transform: translateY(0) scaleX(1); opacity: 1; }\n}\n@keyframes fx-laser-slice {\n  0%, 100% { transform: translateX(0); opacity: .2; }\n  50%      { transform: translateX(5px); opacity: 1; }\n}", "microchip": "<!-- Markup: <div class=\"fx-microchip\">CIRCUIT</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 48 Microchip \u2014 circuit-board traces scroll beneath monospaced letter masks */\n.fx-microchip {\n  font: 600 28px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: transparent;\n  padding: .35em .45em;\n  background:\n    linear-gradient(90deg, transparent 0 44%, var(--ink-3) 44% 47%, transparent 47% 100%),\n    linear-gradient(0deg, transparent 0 46%, var(--ink-2) 46% 49%, transparent 49% 100%),\n    repeating-linear-gradient(90deg, color-mix(in srgb, var(--ink-3) 35%, transparent) 0 1px, transparent 1px 14px),\n    linear-gradient(100deg, var(--ink), var(--ink-3));\n  background-size: 42px 42px, 42px 42px, 14px 100%, 220% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  border: 1px solid color-mix(in srgb, var(--ink-3) 24%, transparent);\n  animation: fx-microchip 3.8s linear infinite;\n}\n@keyframes fx-microchip {\n  to { background-position: 42px 0, 0 42px, 14px 0, -120% 0; }\n}", "heatmap": "<!-- Markup: <div class=\"fx-heatmap\">THERMAL</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 49 Heatmap \u2014 thermal colour blooms migrate across dense letter shapes */\n.fx-heatmap {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: transparent;\n  background:\n    radial-gradient(circle at 20% 50%, #fff26d 0 8%, transparent 24%),\n    radial-gradient(circle at 70% 40%, #ff4f8b 0 12%, transparent 28%),\n    radial-gradient(circle at 44% 78%, var(--ink-3) 0 14%, transparent 32%),\n    linear-gradient(90deg, #2d7dff, #36f0b2, #ffcf4a, #ff4f8b);\n  background-size: 130% 130%, 150% 150%, 140% 140%, 260% 100%;\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-heatmap 3.5s ease-in-out infinite alternate;\n}\n@keyframes fx-heatmap {\n  0%   { background-position: 0 0, 100% 20%, 40% 100%, 100% 0; filter: saturate(1); }\n  100% { background-position: 100% 90%, 0 60%, 80% 0, -100% 0; filter: saturate(1.35); }\n}", "parallax": "<!-- Markup: <div class=\"fx-parallax\" role=\"img\" aria-label=\"LAYERS\"><b aria-hidden=\"true\" style=\"--i:0\">L</b><b aria-hidden=\"true\" style=\"--i:1\">A</b><b aria-hidden=\"true\" style=\"--i:2\">Y</b><b aria-hidden=\"true\" style=\"--i:3\">E</b><b aria-hidden=\"true\" style=\"--i:4\">R</b><b aria-hidden=\"true\" style=\"--i:5\">S</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 50 Parallax \u2014 coloured shadow layers drift at different speeds per letter */\n.fx-parallax {\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-parallax b {\n  font-weight: inherit;\n  display: inline-block;\n  text-shadow:\n    4px 4px 0 color-mix(in srgb, var(--ink-2) 72%, transparent),\n   -4px -3px 0 color-mix(in srgb, var(--ink-3) 72%, transparent);\n  animation: fx-parallax 3s ease-in-out infinite;\n  animation-delay: calc(var(--i) * -.11s);\n}\n@keyframes fx-parallax {\n  0%, 100% {\n    transform: translate(0);\n    text-shadow:\n      4px 4px 0 color-mix(in srgb, var(--ink-2) 72%, transparent),\n     -4px -3px 0 color-mix(in srgb, var(--ink-3) 72%, transparent);\n  }\n  50% {\n    transform: translateY(-4px);\n    text-shadow:\n      8px 2px 0 color-mix(in srgb, var(--ink-2) 72%, transparent),\n     -8px -1px 0 color-mix(in srgb, var(--ink-3) 72%, transparent);\n  }\n}", "inktrap": "<!-- Markup: <div class=\"fx-inktrap\" data-text=\"INK\">INK</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 51 Ink-Trap \u2014 editorial serif-like traps pulse as heavy ink floods corners */\n.fx-inktrap {\n  position: relative;\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .04em;\n  color: var(--ink);\n}\n.fx-inktrap::before,\n.fx-inktrap::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-inktrap::before {\n  color: transparent;\n  -webkit-text-stroke: 1px var(--ink-3);\n  opacity: .65;\n  animation: fx-inktrap-line 2.7s ease-in-out infinite;\n}\n.fx-inktrap::after {\n  color: var(--ink-2);\n  clip-path: polygon(0 0, 100% 0, 84% 22%, 18% 22%, 0 0, 0 100%, 20% 78%, 86% 78%, 100% 100%, 0 100%);\n  mix-blend-mode: screen;\n  animation: fx-inktrap-fill 2.7s ease-in-out infinite;\n}\n@keyframes fx-inktrap-line {\n  50% { transform: scale(1.05); opacity: .95; }\n}\n@keyframes fx-inktrap-fill {\n  0%, 100% { opacity: .18; transform: scale(.98); }\n  50%      { opacity: .72; transform: scale(1.02); }\n}\n\n@keyframes pulse {\n  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 color-mix(in srgb, var(--primary) 50%, transparent); }\n  50% { opacity: .5; box-shadow: 0 0 0 6px transparent; }\n}", "topographic": "<!-- Markup: <div class=\"fx-topographic\">TERRAIN</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 52 Topographic \u2014 contour-map lines crawl through the text fill */\n.fx-topographic {\n  font: 600 28px/1.15 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: transparent;\n  background:\n    repeating-radial-gradient(ellipse at 30% 55%,\n      var(--ink) 0 1px,\n      transparent 1px 7px\n    ),\n    repeating-radial-gradient(ellipse at 78% 35%,\n      var(--ink-3) 0 1px,\n      transparent 1px 8px\n    );\n  background-size: 130px 70px, 170px 80px;\n  -webkit-background-clip: text;\n  background-clip: text;\n  filter: drop-shadow(0 0 8px color-mix(in srgb, var(--ink-3) 25%, transparent));\n  animation: fx-topographic 5s linear infinite;\n}\n@keyframes fx-topographic {\n  to { background-position: 130px 0, -170px 0; }\n}", "signal": "<!-- Markup: <div class=\"fx-signal\" data-text=\"SIGNAL\">SIGNAL</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 53 Signal-Noise \u2014 broadcast interference bends the word into noisy bands */\n.fx-signal {\n  position: relative;\n  font: 600 30px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n  animation: fx-signal 2s steps(1) infinite;\n}\n.fx-signal::before,\n.fx-signal::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-signal::before {\n  color: var(--ink-2);\n  clip-path: inset(0 0 58% 0);\n  animation: fx-signal-a 2s steps(1) infinite;\n}\n.fx-signal::after {\n  color: var(--ink-3);\n  clip-path: inset(42% 0 0 0);\n  animation: fx-signal-b 2s steps(1) infinite;\n}\n@keyframes fx-signal {\n  0%, 21%, 47%, 100% { filter: none; }\n  22%, 24% { filter: blur(.8px) contrast(1.4); }\n}\n@keyframes fx-signal-a {\n  0%, 20%, 52%, 100% { transform: translate(0); opacity: .15; }\n  23% { transform: translate(-8px, -1px); opacity: .9; }\n  54% { transform: translate(5px, 2px); opacity: .7; }\n}\n@keyframes fx-signal-b {\n  0%, 24%, 58%, 100% { transform: translate(0); opacity: .12; }\n  27% { transform: translate(8px, 1px); opacity: .86; }\n  60% { transform: translate(-5px, -2px); opacity: .72; }\n}", "portal": "<!-- Markup: <div class=\"fx-portal\" data-text=\"PORTAL\">PORTAL</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 54 Portal \u2014 a conic vortex opens inside the letters and rotates behind them */\n.fx-portal {\n  position: relative;\n  font: 600 34px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .12em;\n  color: transparent;\n  background: conic-gradient(from 0deg, var(--ink-2), var(--ink), var(--ink-3), var(--ink-2));\n  -webkit-background-clip: text;\n  background-clip: text;\n  animation: fx-portal 3.6s linear infinite;\n}\n.fx-portal::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  color: transparent;\n  background: radial-gradient(circle, transparent 0 30%, #fff 34% 36%, transparent 40% 100%);\n  -webkit-background-clip: text;\n  background-clip: text;\n  opacity: .72;\n  animation: fx-portal-pulse 1.8s ease-in-out infinite;\n}\n@keyframes fx-portal {\n  to { filter: hue-rotate(360deg); transform: rotate(.001deg); }\n}\n@keyframes fx-portal-pulse {\n  0%, 100% { transform: scale(.92); opacity: .24; }\n  50%      { transform: scale(1.08); opacity: .8; }\n}\n\n@keyframes pulse {\n  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 color-mix(in srgb, var(--primary) 50%, transparent); }\n  50% { opacity: .5; box-shadow: 0 0 0 6px transparent; }\n}", "tiltshift": "<!-- Markup: <div class=\"fx-tiltshift\" data-text=\"SHIFT\">SHIFT</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 55 Tilt-Shift \u2014 a sharp focal strip travels through blurred duplicate text */\n.fx-tiltshift {\n  position: relative;\n  font: 600 32px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: color-mix(in srgb, var(--ink) 28%, transparent);\n  filter: blur(2px);\n}\n.fx-tiltshift::before,\n.fx-tiltshift::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n}\n.fx-tiltshift::before {\n  color: var(--ink);\n  filter: blur(0);\n  clip-path: inset(32% 0 32% 0);\n  animation: fx-tiltshift 2.8s ease-in-out infinite alternate;\n}\n.fx-tiltshift::after {\n  color: var(--ink-3);\n  opacity: .3;\n  filter: blur(6px);\n}\n@keyframes fx-tiltshift {\n  0%   { clip-path: inset(0 0 64% 0); transform: translateY(-2px); }\n  100% { clip-path: inset(64% 0 0 0); transform: translateY(2px); }\n}", "duotone": "<!-- Markup: <div class=\"fx-duotone\" data-text=\"DUO\">DUO</div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 56 Duotone \u2014 two offset colour plates slide across a crisp centre layer */\n.fx-duotone {\n  position: relative;\n  font: 600 38px/1.05 \"JetBrains Mono\", monospace;\n  letter-spacing: .1em;\n  color: var(--ink);\n}\n.fx-duotone::before,\n.fx-duotone::after {\n  content: attr(data-text);\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  mix-blend-mode: screen;\n}\n.fx-duotone::before {\n  color: var(--ink-2);\n  animation: fx-duotone-a 2.6s ease-in-out infinite;\n}\n.fx-duotone::after {\n  color: var(--ink-3);\n  animation: fx-duotone-b 2.6s ease-in-out infinite;\n}\n@keyframes fx-duotone-a {\n  0%, 100% { transform: translate(-3px, 0); opacity: .42; }\n  50%      { transform: translate(-9px, -3px); opacity: .8; }\n}\n@keyframes fx-duotone-b {\n  0%, 100% { transform: translate(3px, 0); opacity: .42; }\n  50%      { transform: translate(9px, 3px); opacity: .8; }\n}", "glyphrain": "<!-- Markup: <div class=\"fx-glyphrain\" role=\"img\" aria-label=\"GLYPHS\"><b aria-hidden=\"true\" style=\"--i:0\">G</b><b aria-hidden=\"true\" style=\"--i:1\">L</b><b aria-hidden=\"true\" style=\"--i:2\">Y</b><b aria-hidden=\"true\" style=\"--i:3\">P</b><b aria-hidden=\"true\" style=\"--i:4\">H</b><b aria-hidden=\"true\" style=\"--i:5\">S</b></div> -->\n\n/* Colour tokens \u2014 override to re-skin the effect:\n   --ink    main text colour (defaults to currentColor)\n   --ink-2  primary accent   --ink-3  secondary accent */\n:root { --ink: currentColor; --ink-2: #FF4FD8; --ink-3: #4FF8FF; }\n\n/* 57 Glyph-Rain \u2014 characters fall in staggered vertical trails and re-form */\n.fx-glyphrain {\n  font: 600 28px/1.1 \"JetBrains Mono\", monospace;\n  letter-spacing: .08em;\n  color: var(--ink);\n  display: inline-flex;\n}\n.fx-glyphrain b {\n  font-weight: inherit;\n  display: inline-block;\n  text-shadow:\n    0 -12px 0 color-mix(in srgb, var(--ink-3) 45%, transparent),\n    0 -24px 0 color-mix(in srgb, var(--ink-2) 28%, transparent);\n  animation: fx-glyphrain 2.4s cubic-bezier(.25, .8, .25, 1) infinite;\n  animation-delay: calc(var(--i) * .12s);\n}\n.fx-glyphrain b:nth-child(2n) { color: var(--ink-3); }\n@keyframes fx-glyphrain {\n  0%, 100% {\n    transform: translateY(0);\n    opacity: 1;\n  }\n  38% {\n    transform: translateY(22px);\n    opacity: .22;\n  }\n  64% {\n    transform: translateY(-10px);\n    opacity: .75;\n  }\n}"}}